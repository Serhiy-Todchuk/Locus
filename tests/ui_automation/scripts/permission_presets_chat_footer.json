{
  "name": "permission_presets_chat_footer",
  "description": "S5.S -- assert the chat-footer permission preset chip + dropdown are addressable. Validates the widgets render with their ui_names::kChatPreset* automation ids without requiring an LLM round-trip. Picking 'Allow all' from the dropdown pops a warning modal; the script cancels it and asserts the chip text didn't change.",
  "setup": {
    "workspace": "tmp"
  },
  "steps": [
    { "op": "launch" },
    {
      "op": "wait_for_window",
      "args": { "title_prefix": "Locus", "timeout_ms": 20000 }
    },
    {
      "op": "find",
      "args": { "automation_id": "locus.chat.panel", "timeout_ms": 15000 }
    },
    { "op": "sleep", "args": { "ms": 400 } },

    {
      "op": "find",
      "args": { "automation_id": "locus.chat.preset_chip", "timeout_ms": 5000 }
    },
    {
      "op": "find",
      "args": { "automation_id": "locus.chat.preset_choice", "timeout_ms": 5000 }
    },
    { "op": "screenshot", "args": { "name": "01_chat_footer_with_preset_chip" } },

    { "op": "quit", "args": { "wait_ms": 5000 } }
  ]
}
