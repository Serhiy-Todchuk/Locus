1	Analyze the project, this is your codebase - do you have everything (all the docs understandable and consistent, code is transparent) to perform fixes or write features by yourself.

2	let's test ask_user tool, ask me anything. use your imagination

3	Use search_semantic to find code related to embedding inference

4	Use search_hybrid to find files about file watching and debouncing

5	Add temperature control for LLM to settings dialog in src/gui. Default it to "0.1". Start by reading
CLAUDE.md to get project context, then propose what to change.

6	Test list_directory with deep recursion (e.g., depth=2) on a known directory path, and then test listing a non-existent path.

7	Use search_text with complex FTS5 syntax, such as searching for "error AND critical" OR "timeout near 'seconds'".

8	Run list_directory on the root path ('.') with depth=1 to verify all top-level directories and confirm structural coverage.

9	Test symbol search using a very broad prefix (e.g., `get`) followed by narrowing down the results, or searching for a specific kind/language combination that might not exist.

10  Create an app that render cornel box with global illumination using webgpu. Use multiple files for the project (not everything in one html)


Start a python server for this folder so I an access html files through localhost
Start "python -u -m http.server 8000" command as background process.
run this exact command via run_command_bg:
python -u -c "import sys; sys.stderr.write('TEST OUTPUT\n'); sys.stderr.flush(); import time; time.sleep(60)"

do agentic testing — make Locus build a small C++ tetris minigame in empty folder (D:\Projects\Tetris). If it won't during first run - document what was wrong and continue with next run  and so on untill done

You are an experienced AI programmer/architect who was hired as a QA to test Locus as a C++ coding agent. You've got to push it to the limits of it's capabilities and check if it's good for coding. Think out a few tasks for it (the first could be "create some application/game" in empty folder, the second: fix improve/fix it in another conversation, etc.), write them down first, then make Locus execute them using agentic testing. You task is to find issues and missing things in Locus or things to improve or completelly change (even architecture) and document them afterwards. Your LLM is Qwen 3.6 27B is up and running - this is the best coding LLM I can run on my hardware, so if there are some tools that are too complicated - also notice it - we need to simplify them. The LLM is slow so timeouts could happen. Use "D:\Projects\TestLocalVibe" empty folder as testing Locus workspace. Add /no_think in the end of every prompt. Make sure locus uses ATS/regex/hybrid search (this is considered to be strength of Locus) when excercising coming-back to existing repo task. Ask me a questions if needed. Also make sure to include task that would bloat context with multiple tool calls and messages to test compaction/auto-compaction feature in detail.