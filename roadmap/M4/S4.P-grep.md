# S4.P — Grep Tool (Regex over Raw Content)

## Problem

`search_text` uses FTS5 tokenization — loses punctuation, case, exact identifiers. For `->m_cache`, `TODO(XXX)`, regex patterns, FTS5 is the wrong tool.

## Tasks

- [ ] Ship as `mode=regex` on the unified `search` tool — not a new top-level tool. Add `pattern`, `path_glob?`, `case_sensitive?` to `SearchTool::params()`; dispatch in `SearchTool::execute` next to the existing text/symbols/semantic/hybrid branches. (S3.L collapsed the search family; see [roadmap/M3/S3.L-tool-catalog-hygiene.md](../M3/S3.L-tool-catalog-hygiene.md).)
- [ ] Implementation: `std::regex` over indexed files; respect exclude patterns
- [ ] Decision: keep in-process (no external ripgrep dep) — `std::regex` is good enough at Locus scale
- [ ] Line-numbered output, 2-line context snippet
- [ ] Catch2 tests: special chars, multi-line, case sensitivity
