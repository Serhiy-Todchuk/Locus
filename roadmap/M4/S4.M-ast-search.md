# S4.M — Tree-Sitter Query Tool (Structural Grep)

## Problem

`search_symbols` finds names. Tree-sitter `.scm` queries find *shapes*: all `call_expression` nodes where function is `malloc`, all `if_statement` with empty body, all classes inheriting `ITool`. `ast-grep` built a product on this — killer for refactoring.

## Tasks

- [ ] Ship as `mode=ast` on the unified `search` tool — not a new top-level tool. Add `language` and a Tree-sitter `query` arg (+ optional `path` scope limiter) to `SearchTool::params()`; dispatch in `SearchTool::execute` alongside the other modes. (S3.L collapsed the search family; see [roadmap/M3/S3.L-tool-catalog-hygiene.md](../M3/S3.L-tool-catalog-hygiene.md).)
- [ ] Curated query library per language: `queries/cpp/find-call.scm`, `queries/ts/find-import.scm`, etc.
- [ ] Extend `get_file_outline` to use structured queries instead of ad-hoc node walks
- [ ] LLM-friendly output: match location + 2-line surrounding snippet
- [ ] Catch2 tests: find all `SomeClass()` constructor calls in WS1
