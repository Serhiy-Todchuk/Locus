# S4.S — Telemetry & Agent Performance Metrics

## Problem

Activity panel is per-event. No aggregate metrics: no tokens/s, no avg turn time, no tool-call histogram, no retrieval hit rate. Can't see trends, can't spot regressions.

## Tasks

- [ ] Aggregated metrics tab in activity panel: total tokens in/out, tokens/s (streaming rate), avg / p95 turn time, tool-call counts by name, retrieval top-K hit rate
- [ ] Per-session metrics snapshot saved alongside session JSON
- [ ] Metrics exporter: CSV / JSON for offline analysis
- [ ] Compact chart widgets — no heavy charting library; spark bars only
