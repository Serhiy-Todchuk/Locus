# S6.17 -- Compaction Fixes + Tool-Arg Robustness (TestLocalVibe2 follow-up)

## Status (2026-05-27)

**Shipped** (commits `c776444` + `ba563cc` + `bfaa314`):

| Task | Status | Notes |
|---|---|---|
| A -- Remove `filter_output` | done | Tool deleted, manifest down to 18 entries. |
| B.1 -- Layer escalation | done | `AgentCore::compaction_no_op_streak_` force-enables next layer; count-based heuristic in layer 2 catches large-N small-body tool result shape; hard-trim past oldest_turns deferred. |
| B.2 -- Progress-preserving sentinel | deferred | Generic sentinel still in place. |
| B.3 -- `aggressiveness` preset | core done, GUI deferred | Config + helper + tests landed; settings panel deferred. |
| B.4 -- Telemetry visibility | core done, chip deferred | warn/error log promotion + activity event landed; chip text update needs frontend signature change. |
| C.1 -- Watchdog timer thread | done | 5s tick via `std::thread` + atomic shadow counters. |
| C.2 -- Commit-phase skip | done | Suppresses trips on text-only commit; reasoning-channel-heavy models unaffected (see findings F-NEW-2). |
| D -- `reject_unknown_keys` | done | Helper + alias map + applied to every built-in; read_file header carries applied args; unit tests + lowercase enums for mode / status. Integration replay deferred. |
| E -- `edit_file` shorthand | done | Top-level old_string + new_string normalised to edits=[{...}]. |
| F -- list_directory diagnostic | diagnostic done, fix deferred | Trace fires on IndexedMeta lookup miss; root-cause fix awaits a live trace capture. |
| G -- Split `SearchTool` | deferred | ADR-0008 documents the design. Out of acceptance critical path. |
| H -- `prompt_cost` preset | core done, GUI deferred | Config + helper + tests landed; settings panel deferred. |
| I -- Schema cues in `short_description` | done | Every built-in advertises required args + enum values; 10 unit tests pin the cues. |
| J -- `describe_tool` guarantee | done | Required `name` arg pinned via params() unit test. |
| K -- Polish bucket | deferred | 5 small independent fixes; each can land separately. |
| L -- Index storage shrink | deferred | Different subsystem; out of acceptance critical path. |
| M -- `get_file_outline` freshness | done (2026-05-27, separate commit `292e99b`) | Post-mutation flush + actionable empty-state messaging. |

**Live acceptance run** (2026-05-27 vs qwen/qwen3.6-27b, [findings.md](../../tests/ui_automation/output/agentic_TestLocalVibe2_s617/findings.md)):
- Compaction structurally fixed: the single compaction this run achieved a 10:1 reduction (12136 -> 1339, reached=yes) versus the baseline's three reached=no/no/no no-ops.
- Per-turn fixed cost dropped to ~1476 t (system 682 + manifest 794), confirming the `prompt_cost=balanced` resolution.
- Model used canonical `offset`+`length` on all 5 read_file calls (Task D + Task I steering).
- Pass 5 still didn't produce files on disk -- but the failure mode shifted from "compaction spiraled" (the F17 structural bug this stage was scoped to fix) to "watchdog too tight for Qwen 3.6 27B on this hardware" (a tuning issue documented as F-NEW-1 in findings.md).

**Regression tests** (manual-only, all green 2026-05-27 against `google/gemma-4-e4b`):
- [`tests/integration/test_int_compaction_replay.cpp`](../../tests/integration/test_int_compaction_replay.cpp) (`[s6.17][compaction-replay]`) replays the four Pass-5 archives against the new pipeline; all 4 reach target.
- [`tests/integration/test_int_edit_file_shorthand.cpp`](../../tests/integration/test_int_edit_file_shorthand.cpp) (`[s6.17][edit_file_shorthand]`) live-LLM shorthand round-trip; gracefully handles auto-correcting models.
- [`tests/integration/test_int_read_file_alias_rejection.cpp`](../../tests/integration/test_int_read_file_alias_rejection.cpp) (`[s6.17][read_file_alias]`) live-LLM `lines:` -> "use 'offset' + 'length'" rejection text.
- `tests/integration/test_int_lazy_manifest.cpp` `[s6.17][prompt_cost]` -- prompt_cost=balanced resolves the flag pair AND the per-turn manifest log shrinks to the lazy shape.

## Problem

The 2026-05-25/26 TestLocalVibe2 agentic-mode run on `qwen/qwen3.6-27b` (five passes, 16384-token LM Studio context, full report at [findings.md](../../tests/ui_automation/output/agentic_TestLocalVibe2/findings.md)) exposed two structural problems plus a bag of surface-level bugs that compound under realistic load.

**Compaction is broken on open-ended tasks.** Pass 5 (open-ended refactor: "extract 3 of 10 subsystems into `src/*.h/cpp`") ran for 35.6 min, fired four compactions, and produced **zero output files**. Three of the four compactions reported `reached=no` and stripped nothing -- each "compaction" added only the 37-token sentinel breadcrumb, so the next round was *larger* than the previous one:

```
auto-compact firing (12088 / 13108 = 92% of effective_limit, threshold 85%)
  pipeline compaction 10234 -> 10271 (target 9831, reached=no)    Delta +37
auto-compact firing (13115 / 13108 = 100% of effective_limit, threshold 85%)
  pipeline compaction 11343 -> 11380 (target 9831, reached=no)    Delta +37
auto-compact firing (14316 / 13108 = 109% of effective_limit, threshold 85%)
  pipeline compaction 12427 -> 12464 (target 9831, reached=no)    Delta +37
```

The strip-large-tool-bodies layer (which carried Pass 4's clean 89% reduction) had nothing to strip in Pass 5 because every tool result was a small `run_command` output already trimmed to 60+60 lines by `process_output_filter`. The other layers (drop_redundant_tool_results / drop_old_reasoning / drop_oldest_turns) also had nothing to grab on the conversation shape Pass 5 produced. The pipeline gave up three times in a row while context kept growing, the agent eventually saturated the LLM's 16k ceiling, slow streaming + watchdog 3-strike abort, no files written.

**Lazy tool manifest amplifies silent-fallback bugs.** With the per-turn manifest collapsed to one-line summaries, the model can't see arg names until it calls `describe_tool`. Multiple tools silently swallow unknown keys via `nlohmann::json::value(key, default)`:

- `read_file` with `lines:"100-152"` -> silently returned lines 1-100 (defaulted to `offset=1, length=100`); the agent retried 6 times across Passes 2 and 5 before working around it via PowerShell `Get-Content`.
- `SearchTool` with `type:"regex"` -> silently ran as `mode:"text"`; the resulting FTS5-tokenised search returned 1 hit on a query that genuinely had 4 matching lines.
- `run_command` with `cmd:` -> rejected with `'command' parameter is required` (good error, but the model needed a describe_tool round to discover the right key).

Each individual bug is recoverable. Under realistic load they stack: Pass 5 burned roughly 12 rounds on tool-schema discovery alone before hitting the compaction cliff.

**Removal candidate exposed.** Pass 5 message [15] caught the model reaching for `filter_output` expecting a grep-over-file shape. The tool overlaps with two existing capabilities (inline `output_filter_mode` on `run_command` / `read_process_output`; `search` with `mode=regex`) and its own header concedes "the agent rarely needs to chain a separate filter_output call."

## Lands in M6 because

- Continuation of the [S6.10](S6.10-small-model-robustness.md) small-model robustness theme: same hardware (Qwen 3.6 27B), same goal (keep slow local models productive), same surfaces (AgentLoop, ToolDispatcher, CompactionPipeline, prompt assembly).
- Independent of the connectivity stages (S6.1-S6.9).
- Compaction subsystem refactor is the biggest piece and gates whether agentic-mode on a 16k-context local model is viable for open-ended tasks at all.

## Design

The work splits into a **compaction overhaul** (one large subsection, must land together), a **tool-arg robustness pass** (cross-tool helper + per-tool fixes), a **manifest-shape change** (search modes split per ADR-0008), and a **polish bucket**. Order is set so the highest-impact change lands first.

### Compaction overhaul (Task B -- one cohesive subsection)

Three things must land together because they jointly determine whether the next Pass-5-shaped run actually makes progress. Independent half-fixes are worse than nothing.

1. **Layer escalation on `reached=no`**. The current pipeline runs the configured layers once and reports the outcome. Change: when a compaction reports `reached=no`, the *next* compaction attempt in the same turn force-enables the next-most-aggressive layer regardless of user config, in this fixed order:
   1. `layer_drop_redundant_tool_results`
   2. `layer_strip_large_tool_bodies`
   3. `layer_drop_old_reasoning`
   4. `layer_llm_summary`
   5. `layer_drop_oldest_turns`

   If layer 5 still reports `reached=no`, trim turn-by-turn from oldest until target is hit, and emit a warn-level log line naming the saturated state.

   Also add a **count-based heuristic** to `layer_strip_large_tool_bodies`: when the most recent N rounds carry M+ tool results (today's default config: N=10, M=12), strip the older half regardless of individual byte size. This catches the Pass-5 shape where each result was small but the count was large.

2. **Progress-preserving sentinel**. Today's sentinel is generic: `[Earlier context compacted; full pre-compaction history archived at ...]`. After 4 of these stacked (Pass 5 messages 0-3) the model had no idea what task phase it was in. Two complementary changes:

   - Fire `layer_llm_summary` whenever any *other* layer reports `reached=no`, not just when it's the configured next layer. Prepend the summary to the sentinel: `[Earlier context compacted. Progress so far: <one-sentence summary>. Full pre-compaction history at ...]`.
   - Always preserve the most recent assistant message that has `content` (text) and no `tool_calls` -- the "what I just figured out" notes -- verbatim across compaction. Today's pipeline can strip it under `drop_old_reasoning` / `drop_oldest_turns`; pin it.

3. **Five-layer boolean config -> one preset**. The 5 `layer_*` booleans in `WorkspaceConfig::Compaction` are too granular -- a user can have all defaults "on" and still hit Pass 5's outcome. Replace the surfaced UI with:

   ```
   compaction.aggressiveness = "gentle" | "balanced" | "aggressive"
   ```

   Mapping (the booleans become the under-the-hood matrix the way [S5.S](../M5/S5.S-permission-presets.md) permission_presets works):

   | preset | layers enabled |
   |---|---|
   | gentle | drop_redundant_tool_results + strip_large_tool_bodies |
   | balanced (default) | + drop_old_reasoning + layer_llm_summary |
   | aggressive | + drop_oldest_turns |

   Individual booleans stay parseable from `config.json` for power users / tests. Settings UI surfaces only the preset.

4. **Telemetry / fault visibility**. `reached=no` is currently info-level. Promote to warn the first time it happens in a turn, error if two in a row. The chat-footer `compacted_chip` today shows a count of compaction events; split it so "no-op compactions" are visible separately: `compacted: N (M no-op)`.

### Tool-arg robustness pass (Task D)

A cross-tool helper in `src/tools/shared.h`:

```cpp
std::optional<ToolResult> reject_unknown_keys(
    const ToolCall&                 call,
    std::initializer_list<const char*> allowed);
```

Each affected tool's `execute()` calls it as the first line:

```cpp
if (auto err = reject_unknown_keys(call, {"path", "offset", "length"})) return *err;
```

Error format includes a suggestion when the unknown key is a known alias of an expected one:

> `Error: unrecognized argument 'lines'. Did you mean 'offset' + 'length'? Example: {"path":"...","offset":<int>,"length":<int>}.`

Alias map (lives next to the helper):

| tool | alias seen in this run | suggest |
|---|---|---|
| read_file | `lines`, `line_range`, `range` | `offset` + `length` |
| search* (post-Task-G; pre-Task-G applies to unified search) | `type`, `kind`, `search_mode` | `mode` |
| run_command | `cmd`, `shell`, `exec` | `command` |
| edit_file | (covered by Task E's shorthand acceptance, no alias needed) | -- |
| write_file | `body`, `text` | `content` |

For content-returning tools (read_file, search_text, search_regex), the result header carries the **args actually applied** so the model self-corrects when its arg was honoured-as-default:

> Today: `[snake.cpp lines 1-100 of 152]`
> After: `[snake.cpp lines 1-100 of 152 (offset=1 length=100)]`

### Search modes -> separate tools (Task G + ADR-0008)

See [ADR-0008](../../architecture/decisions/0008-split-unified-searchtool.md). Splits `SearchTool` back into `search_text` / `search_regex` / `search_symbols` / `search_semantic` / `search_hybrid` / `search_ast` as top-level tools. Removes the mode-discriminator dispatch step that is the root of F11 (model didn't reach for symbols mode), F15 (alias arg silently fell through), and F20 (model reached for `filter_output` because `search` didn't read as "grep").

### Prompt-cost preset (Task H)

Today three orthogonal toggles interact: `agent.lazy_tool_manifest` (S6.11), `agent.system_prompt_profile` (S6.12), and `llm.tool_format` (the `openai_json` path triggers S6.10 Task J). ADR-0007's matrix is the right mental model; the user shouldn't have to read an ADR to pick a row.

Add `agent.prompt_cost: minimal | balanced | verbose`:

| preset | lazy_tool_manifest | system_prompt_profile | tool_format hint |
|---|---|---|---|
| minimal | on | minimal | prefer openai_json |
| balanced (default for ctx <= 16k) | on | compact | prefer openai_json |
| verbose (default for ctx > 64k) | off | full | auto |

Individual flags stay parseable from `config.json`; Settings UI surfaces the preset only.

### Watchdog improvements (Task C)

Two narrow fixes, both motivated by clear evidence:

- **Timer-driven tick (F3)**: `AgentLoop::check_watchdog()` is currently called from inside per-chunk stream callbacks only. Pass 1's 499-second silent inter-chunk gap never tripped the 180s watchdog because no chunks arrived to drive the check. Add a 5s `std::thread` timer that calls `check_watchdog()` independent of chunk arrival; stop on stream-end / cancel / round-complete.
- **Don't 3-strike during commit phase (F8)**: Pass 3 and Pass 5 both died because the watchdog tripped during slow text-only summarisation. Heuristic: if the in-flight round has zero accumulated tool_call deltas AND accumulated_text is growing, suppress the auto-nudge and don't count the trip toward the 3-strike cap. Reserve the strikes for "model is reasoning but not committing".

### Removal: `filter_output` (Task A)

Three pieces of evidence:

1. Pass 5 message [15] caught the LLM expecting a file-grep shape; the actual signature (`text` arg, no path) doesn't match the use case.
2. `run_command` and `read_process_output` already trim output inline via `output_filter_mode` / `_pattern` / `_lines` / `_context` -- this covers the >90% case `filter_output` was added for.
3. `search` with `mode=regex` (and post-Task-G, `search_regex`) covers the remaining "extract lines matching X" case directly against indexed files.

Cuts:
- `src/tools/filter_output_tool.h` + `.cpp`
- `register_builtin_tools()` entry in `src/tools/tools.cpp`
- `builtin_tool_category()` entry in `src/tools/permission_presets.cpp`
- Code Map row in `CLAUDE.md`
- Any references in `process_output_filter.h` doc comments

No replacement needed -- the inline filter mode + search_regex cover the call sites.

## Tasks

### Task A -- Remove `filter_output` tool **DONE 2026-05-27** (commit `c776444`)

- [x] Delete [`src/tools/filter_output_tool.h`](../../src/tools/filter_output_tool.h) + `.cpp`.
- [x] Remove registration from [`src/tools/tools.cpp`](../../src/tools/tools.cpp).
- [x] Remove category entry from [`src/tools/permission_presets.cpp`](../../src/tools/permission_presets.cpp).
- [x] Drop the Code Map row in [`CLAUDE.md`](../../CLAUDE.md).
- [x] Drop the "Companion" note in [`src/tools/process_output_filter.h`](../../src/tools/process_output_filter.h) (it references filter_output as the out-of-line form).
- [x] Remove `tests/test_filter_output.cpp` if present. (`tests/test_filter_output_tool.cpp` removed; also dropped `tests/integration/test_int_filter_output.cpp` and `tests/manual/filter-output.md`.)
- [x] Acceptance: `locus_tests` passes (765 cases / 3309 assertions); lazy manifest renders without filter_output; tool roster dropped from 19 to 18.

### Task B -- Compaction overhaul **PARTIAL 2026-05-27** (commit `c776444`)

Four sub-pieces, **must ship as one PR** (half-fixes are worse than nothing for compaction). Most of B shipped; the sentinel composition (B.2) and the GUI/chip wiring (B.3 wxChoice, B.4 chip text) remain.

#### B.1 -- Layer escalation on `reached=no` **DONE**

- [x] [`src/agent/agent_core.cpp`](../../src/agent/agent_core.cpp) `apply_pending_compaction`: tracks `compaction_no_op_streak_` (reset per user turn); after a compaction reports `reached=no`, the next compaction attempt force-enables the next layer in the fixed order (drop_redundant -> strip_large -> drop_old_reasoning -> llm_summary -> drop_oldest_turns) regardless of user config. Warn-logs `CompactionPipeline: escalating to step N`.
- [ ] If layer_drop_oldest_turns still reports `reached=no`, trim oldest turns one at a time until target is hit; emit warn `CompactionPipeline: saturated, hard-trimmed N turns`. **DEFERRED** -- the escalation reaches step 5 but doesn't yet hard-trim past it; live verification on the post-S6.17 acceptance run showed escalation never needed to fire (single compaction reached target), so this guard hasn't been exercised yet.
- [x] Count-based heuristic in `layer_strip_large_tool_bodies`: when the message vector carries >= `count_heuristic_threshold` (default 12) tool results, strip the older half regardless of byte size. Catches the Pass-5 shape where every result was small (filtered run_command outputs) but the count grew unbounded, leaving the byte-threshold pass with nothing to chew on. Knobs in [`WorkspaceConfig::Compaction`](../../src/core/workspace_config.h) + threaded through `CompactionLayerSelection`. Live verified: drops archive #2 from 10234 -> 6815 tokens, #3 from 11343 -> 7458, #4 from 12427 -> 8071, each reaching the 9831 target on a single pipeline call.
- [x] Pass-5-replay regression test `tests/integration/test_int_compaction_replay.cpp` (tag `[s6.17][compaction-replay]`, commit `bfaa314`): loads all four archived Pass-5 histories at `tests/ui_automation/output/agentic_TestLocalVibe2/history.before-compact-*.json` and asserts each reaches the original 9831-token target with `aggressiveness=balanced` + live LLM for the summary layer. Manual-only. **Verified green 2026-05-27 against `google/gemma-4-e4b`** -- all 4 archives reach target.

#### B.2 -- Progress-preserving sentinel **DEFERRED**

- [ ] Pipeline change: when ANY layer reports `reached=no`, fire `layer_llm_summary` unconditionally (today it's gated separately). Prepend the summary to the sentinel string:
  > `[Earlier context compacted. Progress so far: <summary>. Full pre-compaction history at ...]`
- [ ] Pin the most recent assistant message that has `content` (text) and no `tool_calls` -- it survives `drop_old_reasoning` / `drop_oldest_turns`. Implement as a "preserved set" the pipeline layers check before stripping.
- [ ] Unit test: replay the Pass 5 archive; verify the post-compaction history carries (a) a `[Earlier context compacted. Progress so far: ...]` sentinel rather than the generic one, (b) the last assistant-text message verbatim.

#### B.3 -- `compaction.aggressiveness` preset **CORE DONE**, GUI deferred

- [x] [`src/core/workspace_config.h`](../../src/core/workspace_config.h) gains `std::string aggressiveness = "balanced"` in the Compaction sub-struct.
- [x] [`src/core/workspace_config_json.cpp`](../../src/core/workspace_config_json.cpp) reads/writes the field.
- [x] New helper `layers_for_aggressiveness(preset)` in [`src/agent/compaction_pipeline.h`](../../src/agent/compaction_pipeline.h) returns the `{layer_*: bool}` matrix; `selection_from_config` reads from the matrix when `aggressiveness != "custom"`, falls back to individual booleans when `aggressiveness=="custom"`.
- [ ] [`src/frontends/gui/settings_dialog.cpp`](../../src/frontends/gui/settings_dialog.cpp): Compaction settings tab gets a `wxChoice` with the three preset names; individual booleans hidden behind an "Advanced" expander. **DEFERRED** -- power users can edit `.locus/config.json` directly today.
- [x] Unit tests in `tests/test_compaction_pipeline.cpp` `[s6.17]`: each preset name maps to the expected layer matrix; `selection_from_config` preset overrides the individual `layer_*` booleans unless `aggressiveness=="custom"`; WorkspaceConfig JSON round-trip carries `aggressiveness` + `count_heuristic_*`.

#### B.4 -- Telemetry visibility **CORE DONE**, chip update deferred

- [x] `reached=no` log line promotes from info -> warn for first occurrence per turn, error for second consecutive within turn (implementation in `AgentCore::apply_pending_compaction`).
- [ ] [`src/frontends/gui/chat_panel.cpp`](../../src/frontends/gui/chat_panel.cpp): `compacted_chip` text changes from `compacted: N` to `compacted: N (M no-op)` when M > 0. Tooltip on the chip explains what no-op means. **DEFERRED** -- would widen the `IFrontend::on_compaction_archived` signature; deferred to keep the PR focused. `compaction_no_op_total_` is tracked on `AgentCore` so the wire-up is a one-line frontend call when the panel piece lands.
- [x] Activity log emits a `compaction_no_op { layer_attempted: <name>, before: <int>, after: <int> }` event distinct from the existing `compaction` success event (implemented via the existing `ActivityKind::compaction` event with a "Compaction no-op (target not reached)" summary; a follow-up could add a distinct `ActivityKind` enum value if frontend filtering needs it).

### Task C -- Reasoning watchdog improvements **DONE 2026-05-27** (commit `c776444`)

#### C.1 -- Timer-driven check (F3) **DONE**

- [x] [`src/agent/agent_loop.cpp`](../../src/agent/agent_loop.cpp): added a 5s timer thread that ticks `check_watchdog()` while a stream is in flight. Thread starts at round start (when `wd_max_seconds > 0 || wd_max_chars > 0`), stops on stream-end / cancel / round-complete via `watchdog_timer_stop` atomic + condition_variable.
- [x] Synchronisation: introduced atomic shadow counters (`text_chars_atomic`, `reasoning_chars_atomic`, `tool_call_seen_atomic`, `watchdog_tripped_atomic`) updated alongside the string appends in `on_token` / `on_reasoning_token`. The check_watchdog lambda reads via the atomics so the timer thread is safe to call into. Single-trip via `compare_exchange_strong`.
- [ ] Unit test against a recorded silent-stream stream (mock: no chunks for 200s); verify the watchdog trips on the timer, not on a chunk. **DEFERRED** -- the existing `tests/test_reasoning_watchdog.cpp` covers the chunk-callback trip path; a separate mock-stream test for the silent-stream case is worth adding but the live acceptance run already exercised the timer thread (it tripped twice at chars=134 and chars=52 between chunk arrivals during the 21-min Pass-5 run, see findings.md).

#### C.2 -- Don't 3-strike during commit phase (F8) **DONE**

- [x] In `check_watchdog()`: if `!tool_call_seen` AND `text_chars > 100` AND `reasoning_chars == 0`, the model is committing to text-only output. Suppress the trip; don't fire `on_reasoning_watchdog_tripped`, don't increment the nudge counter. Logs info-level `watchdog tick during commit phase ... suppressed`.
- [ ] Unit test: mock stream with text-only tokens past the threshold -> watchdog stays silent; mock stream with reasoning-only tokens past the threshold -> watchdog fires. **DEFERRED** -- same reason as C.1.

**Note from acceptance run**: the commit-phase skip didn't suppress any trips against Qwen 3.6 27B because that model streams via the reasoning channel for tool-calling rounds (reasoning_chars > 0 fails the `reasoning_chars == 0` precondition). The skip works for the documented shape (final-answer text-only stream) but doesn't help reasoning-channel-heavy local models. See findings.md F-NEW-2.

### Task D -- Stop silent-fallback on unknown tool args **DONE 2026-05-27** (commit `c776444`)

- [x] [`src/tools/shared.h`](../../src/tools/shared.h) + `.cpp`: new helper
  ```cpp
  std::optional<ToolResult> reject_unknown_keys(
      const ToolCall&                    call,
      std::initializer_list<const char*> allowed);
  ```
  with an internal alias map (table in `.cpp`) that turns `lines`/`type`/`cmd`/`body` into helpful suggestions. Per-tool entries cover read_file (`lines` / `line_range` / `range` / `start` / `end` -> offset+length), search (`type` / `kind` / `search_mode` -> mode), run_command + run_command_bg (`cmd` / `shell` / `exec` -> command), write_file (`body` / `text` / `data` -> content). Unknown keys without an alias entry get a generic "Call describe_tool('<name>') for the schema" hint.
- [x] Apply to each affected tool's `execute()` as the first line: `read_file`, `write_file`, `edit_file`, `delete_file`, `search`, `run_command`, `run_command_bg`, `read_process_output`, `stop_process`, `list_directory`, `get_file_outline`, `describe_tool`, `add_memory`, `search_memory`, `ask_user`, `propose_plan`, `mark_step_done`. (`list_processes` takes no args -- nothing to reject. Per-mode search_* tools are post-Task-G; while G is deferred the unified `search` tool's reject covers the union of every per-mode arg + `name` for the symbols-delegate path.)
- [x] Lowercase enum-shaped string args before comparison: `mode` in `SearchTool`, `status` in `MarkStepDoneTool`. Single `std::transform` call per arg.
- [x] Content-returning tools embed the actually-applied args in the result header: `ReadFileTool` now prints `[snake.cpp lines 1-100 of 152 (offset=1 length=100)]`. Search tools deferred to Task G's per-mode split (the unified `search` header was not extended).
- [x] Unit tests `tests/test_tool_arg_validation.cpp` `[s6.17][tool_arg_validation]`: per-tool unknown-key rejection (read_file `lines`, search `type`, write_file `body`, run_command `cmd`), alias suggestion content, lowercase-enum tolerance (search `mode: "TEXT"`), args-in-header for read_file, generic hint for keys without aliases.
- [ ] Integration test: replay Pass 5's `read_file lines="100-152"` shape; confirm the new error message is returned instead of silent lines-1-100 fallback. **DEFERRED** -- unit test exercises the rejection path against the real ReadFileTool; a live-LLM replay is worth adding alongside the Pass-5 compaction-replay test in a follow-up. The 2026-05-27 acceptance run confirmed the model used canonical offset+length on all 5 read_file calls without needing to test the rejection path.

### Task E -- `edit_file` single-edit shorthand (F5) **DONE 2026-05-27** (commit `c776444`)

- [x] [`src/tools/file_tools.cpp`](../../src/tools/file_tools.cpp) `EditFileTool::execute`: when `args` has no `edits` key but does have top-level `old_string` AND `new_string`, normalise into `edits=[{old_string, new_string, replace_all?}]` before dispatching.
- [x] Unit tests in `tests/test_tools.cpp` `[s6.17][edit_file]`: single-edit shorthand returns the same result as the explicit array form; shorthand honours top-level `replace_all`.

### Task F -- `list_directory [unindexed]` diagnostic + fix (F12) **DIAGNOSTIC DONE**, fix deferred

- [x] [`src/tools/index_tools.cpp`](../../src/tools/index_tools.cpp) `ListDirectoryTool::execute`: when the IndexedMeta map is empty for a non-empty FS path, emit a trace `list_directory: IndexedMeta lookup miss for rel='<path>'; files-table keys (sample of N): [...]`. (Note: lives in the tool layer, not `index_query.cpp`, since both the FS walk and the index lookup are visible there.)
- [ ] Run the diagnostic against TestLocalVibe2. Two likely outcomes:
  - Path-key mismatch (forward-slash vs backslash, case, etc.) -> canonicalise the key at insert OR at lookup (consistent across both call sites).
  - Missing-row condition (files_fts has rows but `files` doesn't) -> investigate `Indexer::index_file` write path; the two writes should be in the same transaction.
- [ ] Acceptance: re-run `list_directory` against TestLocalVibe2 (3 files, all indexed); annotation should read `[cpp]` for `snake.cpp` and `big_module.cpp`, `[text]` for `CMakeLists.txt`. NOT `[unindexed]` for any of them. **DEFERRED** -- the diagnostic is in place; the root-cause investigation + fix requires running the trace against a real workspace and seeing which of the two outcomes fires. Task M's post-mutation flush in the dispatcher already absorbs much of the F12 surface (agent-written files appear indexed in the next round); the remaining cases are stale-on-launch which the diagnostic will pinpoint.

### Task G -- Split unified `SearchTool` into per-mode tools **DEFERRED**

Out-of-scope from the Pass-5 acceptance critical path; deferred to keep this PR focused. ADR-0008 documents the design. Picks up cleanly when scheduled.

See [ADR-0008](../../architecture/decisions/0008-split-unified-searchtool.md) for the design rationale and rejected alternatives.

- [ ] [`src/tools/search_tools.cpp`](../../src/tools/search_tools.cpp): remove the `SearchTool` dispatcher class; the per-mode classes (`SearchTextTool`, `SearchRegexTool`, `SearchSymbolsTool`, `SearchSemanticTool`, `SearchHybridTool`, `SearchAstTool`) become the direct registry entries.
- [ ] Each per-mode class's `name()` returns its specific name (`search_text`, etc.).
- [ ] Each per-mode class's `short_description()` carries its specific signature: `"search_text(query, path_glob?, max_results?) -- FTS5 over indexed text content"` etc.
- [ ] `register_builtin_tools()` in `src/tools/tools.cpp` adds the six entries instead of one.
- [ ] `builtin_tool_category()` in `src/tools/permission_presets.cpp` maps each `search_*` to `"read"`.
- [ ] Backward-compatibility shim: when loading a saved session (`SessionManager::load`), translate `tool_calls[].name == "search"` with a `mode` arg into the appropriate `search_<mode>` name + drop the mode arg. Single-version shim; remove next milestone.
- [ ] Update `tests/test_search.cpp`: per-tool test classes inherit from a shared `[s6.17]` tag.
- [ ] Update doc references: CLAUDE.md Code Map ("Search subsystem: 6 tools, one per mode"), `architecture/workspace-index.md`, `tests/integration/test_int_search.cpp` tags.
- [ ] Acceptance: `locus_tests` passes; lazy manifest renders six search entries; an LLM call with `name="search_regex"` round-trips end-to-end.

### Task H -- `agent.prompt_cost` preset **CORE DONE 2026-05-27** (commit `c776444`), GUI deferred

- [x] [`src/core/workspace_config.h`](../../src/core/workspace_config.h) Agent struct gains `std::string prompt_cost = ""`. Empty = use individual flags (back-compat).
- [x] `workspace_config_json.cpp` reads/writes; `prompt_cost_apply(cfg)` resolves the preset onto the underlying flags during config load.
- [x] New helper `prompt_cost_to_flags(preset, context_limit, fallback_lazy, fallback_profile)` returns the `{lazy_tool_manifest, system_prompt_profile}` pair. (Dropped the `tool_format_hint` slot from the spec; it would have required a separate setting and the OpenAI/Auto formats already auto-select based on the model.) The "default" mapping branches on `context_limit`: <= 16k -> balanced; > 64k -> verbose; in-between -> honour the individual flags.
- [ ] [`src/frontends/gui/settings_dialog.cpp`](../../src/frontends/gui/settings_dialog.cpp): LLM settings tab gets a `wxChoice` for prompt_cost; individual flags hidden behind "Advanced". **DEFERRED** -- power users can edit `.locus/config.json` directly today.
- [x] Unit tests in `tests/test_lazy_manifest.cpp` `[s6.17][prompt_cost]`: each preset name maps to the expected flag pair; "default" branches on context size; empty preset preserves individual flags; `prompt_cost_apply` overrides individual flags; JSON round-trips. Verified live in the 2026-05-27 acceptance run -- log line `system prompt ~682 tokens (profile=compact)` + `Tool manifest: 14 tools, ~794 tokens` confirms the `balanced` preset resolved correctly.

### Task I -- Surface schema cues in `short_description` **DONE 2026-05-27** (commit `c776444`)

Architectural note from findings.md: with lazy_tool_manifest collapsing the manifest, every "I guessed the wrong arg shape" round costs an extra describe_tool. Two-line tweak per tool to name required args + enum values inline in `short_description()`.

- [x] File tools (in [`src/tools/file_tools.h`](../../src/tools/file_tools.h)): `read_file(path, offset=1, length=100) -- read a line range.`, `write_file(path, content, overwrite=false) -- ...`, `edit_file(path, edits=[{old_string,new_string,replace_all?}]) -- ...`, `delete_file(path) -- ...`.
- [x] Search tool (still unified pre-Task-G): `search(query, mode=text|regex|symbols|ast|semantic|hybrid) -- workspace search.`
- [x] Process tools: `run_command(command) -- ...`, `run_command_bg(command) -- ...`, `read_process_output(process_id) -- ...`, `stop_process(process_id) -- ...`, `list_processes() -- ...`.
- [x] List / outline / describe / memory / plan / ask_user tools likewise.
- [x] Unit test `tests/test_short_descriptions.cpp` `[s6.17][short_description]`: for each built-in tool, `short_description()` mentions every required arg name and every enum-valued arg's allowed values (10 cases, one per tool family).

### Task J -- `describe_tool` meta-tool guarantee **DONE 2026-05-27** (commit `c776444`)

Architectural note: when `lazy_tool_manifest=true`, the model needs `describe_tool` itself to remain fully described in the manifest -- it's the meta-tool that recovers from manifest collapse.

- [x] Verified in [`src/tools/tool_registry.cpp`](../../src/tools/tool_registry.cpp) `build_entry`: the `keep_full_schema = !lazy || t.name() == "describe_tool"` line is the explicit exception. Same gate in the lazy-mode short_description swap (also at line `if (lazy && t.name() != "describe_tool")`).
- [x] Verified same for the prompt-side "## Available Tools" section when it's rendered ([`src/agent/system_prompt_assembly.cpp`](../../src/agent/system_prompt_assembly.cpp) `describe_tool` entry-point bullet).
- [x] Unit test `tests/test_describe_tool.cpp` `[s6.17][describe_tool][lazy_manifest]`: pins `describe_tool->params()` exposes a required `string`-typed `name` arg. Companion to the existing `tests/test_lazy_manifest.cpp` checks that round-trip the same guarantee through the schema-build path.

### Task L -- Index storage shrink (vec0 lazy alloc + chunker leaf-symbol fix) **DEFERRED**

Out-of-scope from the Pass-5 acceptance critical path (different subsystem -- chunker / vectors.db rather than compaction / manifest). Picks up cleanly when scheduled.


The 2026-05-26 follow-up investigation against the TestLocalVibe2 vectors.db found two distinct bloat sources stacked behind the original F2 finding (4.5 MB vectors.db-wal):

- `chunk_vectors_vector_chunks00` is allocated **exactly 4 MiB** the moment the vec0 table is created (sqlite-vec stores vectors in fixed-size 1024-vector buckets). For any workspace with `semantic_search.enabled=false`, or with zero indexable files, or with <1024 chunks total, this 4 MiB sits empty.
- `chunks` table had **238 rows for a 3-file workspace** with **220 of them overlapping at least one other in the same file** and **43 rows carrying <=50 bytes of content (13 of which are zero bytes)**. Root cause is in `chunk_code` ([src/index/chunker.cpp](../../src/index/chunker.cpp)): the symbol extractor emits 112 symbols for big_module.cpp with 111 nested inside another (namespace > class > methods), and the chunker's walk has two bugs that interact poorly with nested input -- degenerate chunks when the cursor-clamp inverts the range, and cursor moving backward when a nested symbol ends earlier than the current cursor. Detailed trace in the 2026-05-26 conversation log.

#### L.1 -- Lazy vec0 table creation

- [ ] [`src/core/database.cpp`](../../src/core/database.cpp) `Database::ensure_vectors_schema(dim)`: defer the `CREATE VIRTUAL TABLE chunk_vectors USING vec0(...)` statement (and the memory_vectors equivalent) until the first insert. Today's "create on workspace open" path pays 4 MiB + 4 MiB = 8 MiB up front; lazy creation drops this to 0 for workspaces with no embeddings.
- [ ] Implementation shape: track schema state with a boolean `vectors_table_present_`; first insert path checks the flag, runs the CREATE if false, then proceeds with the INSERT. Reads check the same flag and short-circuit to "no results" if the table isn't there yet (the existing query path's "table doesn't exist" branch should already handle this; verify).
- [ ] Acceptance: open an empty workspace with `semantic_search.enabled=true`; `vectors.db` size on disk after first launch is < 100 KB (today: 4.0+ MiB). Index a single small file; `vectors.db` grows to the expected size after the embedding worker drains.

#### L.2 -- Chunker `chunk_code` defensive walk

- [ ] [`src/index/chunker.cpp`](../../src/index/chunker.cpp) `chunk_code` loop: after the `sym_begin < cursor -> sym_begin = cursor` clamp, add the inverse check `if (sym_begin >= sym_end) continue;` -- this drops nested symbols entirely instead of emitting a degenerate `start_line > end_line, content=""` chunk and moving the cursor backward.
- [ ] Unit test `tests/test_chunker.cpp` `[s6.17]`: input with a namespace spanning the whole file + several nested classes + nested methods -- verify no chunk has `start_line > end_line`, no chunk has empty `content`, and the cursor never moves backward (assert on per-chunk monotonicity).

#### L.3 -- Leaf-symbol pre-filter in indexer

- [ ] [`src/index/indexer.cpp`](../../src/index/indexer.cpp) around line 479 (the `if (!symbol_spans.empty())` branch before `chunk_code`): pre-filter `symbol_spans` to drop spans that fully contain another span. O(n^2) check; n is the per-file symbol count which is typically <500. Pseudocode:
  ```cpp
  std::vector<SymbolSpan> leaf_spans;
  for (size_t i = 0; i < symbol_spans.size(); ++i) {
      const auto& s = symbol_spans[i];
      bool contains_another = false;
      for (size_t j = 0; j < symbol_spans.size(); ++j) {
          if (i == j) continue;
          const auto& other = symbol_spans[j];
          if (s.line_start <= other.line_start && s.line_end >= other.line_end
              && (s.line_start < other.line_start || s.line_end > other.line_end)) {
              contains_another = true;
              break;
          }
      }
      if (!contains_another) leaf_spans.push_back(s);
  }
  chunks = chunk_code(content, leaf_spans, ...);
  ```
- [ ] Acceptance: re-index TestLocalVibe2 against the fixed code. `SELECT COUNT(*) FROM chunks WHERE file_id=3` drops from 230 to ~100. `SELECT COUNT(*) FROM chunks WHERE LENGTH(content) = 0` is 0. The 220/238 overlap rate measured 2026-05-26 falls to <10% (a small natural overlap from the existing `overlap` parameter is expected and fine).
- [ ] Side benefit: embedder workload halves on code-heavy files -- 230 forward passes -> ~100 for big_module.cpp.

#### L.4 -- Skip empty chunks defensively

- [ ] [`src/index/chunker.cpp`](../../src/index/chunker.cpp) `make_chunk`: refuse to emit a chunk where `begin_idx >= end_idx`. Belt-and-suspenders behind L.2.
- [ ] [`src/index/indexer.cpp`](../../src/index/indexer.cpp) chunk-insert loop: skip any chunk with `content.empty()` before persisting. Belt-and-suspenders behind L.3.
- [ ] Unit test: feed `make_chunk` with `begin >= end` -- expects either a thrown assertion or an empty optional, never a stored row.

#### L.5 -- Fix `chunk_vectors` UNIQUE-constraint warnings (rowid-reuse race)

Surfaced 2026-05-27 during the full integration run (74/74 passed; the warning fired 3 times for chunk_ids 21221, 21397, 21401). Root cause: `chunks.id` is `INTEGER PRIMARY KEY` *without* `AUTOINCREMENT`, so SQLite reuses freed rowids after `DELETE FROM chunks WHERE file_id=N`. When a file is modified faster than the embedding worker drains the queue, the new chunks get the same ids as the old ones, the worker sees duplicates, and the second `INSERT INTO chunk_vectors` collides with the first. In production this drops the second embedding silently -- the rare-but-real correctness bug under it is that the embedding for the *current* chunk content is the one that gets lost.

- [ ] [`src/index/embedding_worker.cpp`](../../src/index/embedding_worker.cpp) line 95: change `INSERT INTO chunk_vectors` to `INSERT OR REPLACE INTO chunk_vectors`. Keeps the embedding for the current chunk content rather than dropping it on the conflict.
- [ ] Demote the warning at line 150 (`Failed to insert vector for chunk N: UNIQUE constraint failed`) to trace level, OR remove it (the OR REPLACE path no longer triggers it). Other SQLite errors should still surface.
- [ ] Optional follow-up (different line count -- could be deferred): `EmbeddingWorker::enqueue` deduplicates against a `std::unordered_set<int64_t> in_queue_` so the worker doesn't compute the same embedding twice in a rapid-re-index burst. ~20 lines including the lock dance. Not strictly necessary -- OR REPLACE makes the redundant work harmless -- but saves an embedder forward pass per duplicate.
- [ ] Unit test `tests/test_embedding_worker.cpp` `[s6.17]`: enqueue the same chunk_id twice with different content blobs; assert no warning fires and the row in `chunk_vectors` carries the *second* content's embedding.
- [ ] Integration test: replay the rapid-re-index pattern (write file -> wait for embed start -> write file again before drain) and assert zero UNIQUE-constraint warnings in `.locus/locus.log`.

Out of scope for L (per user 2026-05-26): filtering chunks below a minimum byte/line size -- the small chunks remain useful for document-workspace headings (e.g. a one-line H2 heading is a legitimate retrieval anchor even at 30 bytes). L.1-L.4 fix the structural defects without touching the size policy.

Acceptance for the whole task: vectors.db on TestLocalVibe2 (3 files) drops from 4.25 MB to ~1 MB (one vec0 bucket carrying ~100 vectors plus chunks-table content); WAL after clean shutdown stays at 0; no chunk has degenerate line ranges or empty content.

### Task M -- `get_file_outline` mid-turn freshness + empty-state messaging (F7)

Investigation 2026-05-27 (see [tests/test_outline_race.cpp](../../tests/test_outline_race.cpp) for the deterministic repro). F7's "0 entries for a valid C++ file" is not a symbol-extractor bug -- the cpp extractor handles `function_definition` / `struct_specifier` / `class_specifier` / `enum_specifier` / `namespace_definition` correctly. The failure mode is mid-turn index staleness:

- [`main.cpp`](../../src/main.cpp#L373) and [`locus_frame.cpp`](../../src/frontends/gui/locus_frame.cpp#L568) call `WatcherPump::flush_now()` exactly once per USER turn, at prompt submit.
- Inside a turn, the watcher pump's 1.5 s quiet-period batches indexing. Agent round N writes `snake.cpp` via `write_file`; agent round N+1 calls `get_file_outline('snake.cpp')` before the pump has fired `process_events`. The `symbols` table is empty for that path; the old code returned the silent `outline (0 entries)` string.
- Same race silently masks `search` mode=symbols hits and `list_directory` annotations on agent-written files (the latter is the surface F12 was diagnosing).

The fix has two halves and resolves F7 without touching the extractor schema or folding the tool into `search_symbols`.

#### M.1 -- Post-mutation flush in the dispatcher

- [x] [`src/agent/tool_dispatcher.cpp`](../../src/agent/tool_dispatcher.cpp): after a successful tool whose `builtin_tool_category` is `edit` or `delete`, call `services_.workspace()->watcher_pump().flush_now()`. Logs trace `ToolDispatcher: flush_now after mutating tool '<name>'`. Failure is warn-logged but not propagated -- the tool result is unaffected.
- [x] Cost: one mutex acquire + `drain_all` (empty on most calls) on the agent thread. Same code path the per-turn flush already exercises.
- [x] Side-benefit: `search` mode=symbols and `list_directory` annotation on agent-written files also get the fresh view in the next round -- partially absorbs the F12 surface (Task F's diagnostic still tells us whether path-key mismatch is a separate root cause).

#### M.2 -- Actionable empty-state messages in `get_file_outline`

- [x] [`src/tools/index_tools.cpp`](../../src/tools/index_tools.cpp) `GetFileOutlineTool::execute`: when the index returns zero entries, branch on filesystem reality before rendering the response:
  - file does NOT exist on disk -> `error_result("Error: file not found at '<path>'...")`
  - file exists, size > `index.max_file_size_kb` cap -> `"File is N bytes -- above the index size cap (M bytes); not indexed. Use read_file..."`
  - file exists, otherwise -> enumerated possible causes (race / unrecognised language / no top-level decls) + concrete suggestions (`search` mode=text, `read_file`)
- [x] Successful outline responses are unchanged.

#### M.3 -- Test coverage

- [x] [`tests/test_outline_race.cpp`](../../tests/test_outline_race.cpp) (tag `[outline-race]`): four cases -- baseline (pre-indexed file works), race (mid-turn write returns the actionable message), fix (flush_now resolves it and symbols appear), nonexistent path (clear error). Asserts no test ever sees the old silent `(0 entries)` string except via the historical paper trail.
- [x] Full suite: 748 cases / 3247 assertions pass.

### Task K -- Polish bucket **DEFERRED**

Small fixes that don't fit elsewhere but were caught in the test run. Each is independent and can land in any follow-up PR.

- [ ] **F1**: [`tests/ui_automation/locus-op.ps1`](../../tests/ui_automation/locus-op.ps1) default `$TimeoutMs` 30000 -> 600000. Update [`tests/ui_automation/AGENTIC_TESTING.md`](../../tests/ui_automation/AGENTIC_TESTING.md) PowerShell helper section to note "-TimeoutMs" for long waits.

- [ ] **UIA-global-state isolation** (surfaced 2026-05-27 during the post-test debugging): wxAUI perspective is saved to **global** `<global_dir>/ui_state.json` (e.g. `~/.locus/ui_state.json`), not per-workspace -- see [`src/frontends/gui/ui_state.cpp`](../../src/frontends/gui/ui_state.cpp). One failed UIA script that exits with a pane in non-default state pollutes every subsequent Locus launch, including fresh `tmp` workspaces. During the 2026-05-27 UIA debug pass this manifested as the Memory Bank panel being visible at baseline in a `tmp`-workspace script run because the previous failed run had left it shown at exit. Two options:
   1. (Recommended) Persist `aui_perspective` per-workspace at `<workspace>/.locus/ui_state.json` instead of in the global dir. Workspace-specific layout is more useful anyway (user wants different docking for code vs. notes workspaces). The window-size + maximized-state stay global -- those are screen-bound, not workspace-bound.
   2. (Cheap mitigation) Have `--workspace tmp` ALWAYS clear the global ui_state.json's `aui_perspective` field at launch, so every tmp-workspace run starts from the AUI default. Doesn't fix the broader "I changed one workspace's layout and another forgot what it was" problem but unblocks the UIA suite.
- [ ] **F2**: [`src/index/embedding_worker.cpp`](../../src/index/embedding_worker.cpp): when the queue drains, run `PRAGMA wal_checkpoint(PASSIVE)` on the vectors.db connection. Add the same to `Indexer::shutdown()` for the main.db connection. Stops the 4.5 MB-per-empty-workspace WAL bloat.
- [ ] **F4**: AGENTIC_TESTING.md addition under "Wire protocol": one paragraph noting that ops serialise server-side -- intermixing reads with `wait_for_agent_idle` causes the intermixed call to time out behind the wait. Recommend using the helper's `-TimeoutMs` and queuing reads after the wait returns.
- [ ] **F10 / F16**: search result granularity divergence. With Task G in place, this becomes per-tool documentation: `search_text` returns "one row per file (FTS5 snippet)", `search_regex` returns "one row per match (capped at max_results)". Document in each tool's `description()` (the full version, not short).
- [ ] **F14**: covered by Task D.3 (lowercase enum-shaped args). Listed here only for traceability.

## Implementation order

The dependency graph is shallow; this is mostly a quality-of-life ordering.

1. **Task A** (filter_output removal) -- standalone, no risk, ship first.
2. **Task B** (compaction overhaul) -- highest impact; whole stage rests on it. Regression test replaying Pass 5 archive must be green.
3. **Task D** (silent-fallback rejection) -- foundational; needs Task G's tool names but the helper itself can land first.
4. **Task C** (watchdog timer + commit-phase skip) -- isolated to AgentLoop.
5. **Task E** (edit_file shorthand) + **Task J** (describe_tool meta-test) -- tiny; piggyback on whatever PR.
6. **Task F** (list_directory diagnostic) -- diagnostic first; fix depends on what the trace shows.
7. **Task I** (short_description content) -- many small edits; doable in one PR per tool family.
8. **Task H** (prompt_cost preset) -- depends on Task I being mostly done (the preset surfaces tools as a group).
9. **Task G** (search split) -- biggest tool-surface change. ADR-0008 first, then implementation. Touches Task D's alias map (removes `type`/`kind`/`search_mode` rows once search is split).
10. **Task L** (index storage) -- L.1 (lazy vec0) is standalone; L.2-L.4 (chunker fix) touch a separate subsystem from compaction/manifest and can land any time. Re-indexing TestLocalVibe2 after L lands is part of stage acceptance.
11. **Task M** (get_file_outline freshness) -- shipped 2026-05-27, separate commit. Dispatcher post-mutation flush also helps Task F's surface.
12. **Task K** (polish) -- pick up alongside whatever lands first.

## Out of scope (explicit)

- **`/no_think` auto-append knob** -- handled by [S6.14](S6.14-thinking-knob.md) (Thinking ON/OFF/auto Knob). That stage's per-model-family injection covers the same need.
- **Memory bank / MCP / plan mode / CLI** -- no data from this run. Any related changes belong in their own stages.
- **Layer-llm-summary prompt quality** -- the layer fires more often after Task B.2, but its actual summary prompt is unchanged. Quality of the summaries themselves is a separate observation; this stage assumes the existing summary is good enough.
- **Pre-Task-G regex/text result granularity unification** -- F10 calls out divergence. Task G splits them into separate tools where the divergence is naturally documented per-tool; the deeper "should regex mode return per-line and text mode also return per-line via FTS5 offsets()" question is deferred.
- **Search modes for AST queries** -- ADR-0008 keeps `search_ast` as one of the six tools; this stage does not touch its query language or capability beyond renaming.

## Acceptance for the stage

- Replay Pass 5's archived sessions (`tests/ui_automation/output/agentic_TestLocalVibe2/history.before-compact-*.json`) against the new compaction pipeline; each of the four `reached=no` events resolves to a productive compaction.
- Re-run the TestLocalVibe2 agentic harness end-to-end against `qwen/qwen3.6-27b`. Pass 5 (open-ended refactor) MUST produce files on disk by end-of-turn; this is the smoke test for the stage.
- No new findings of severity H/M from the re-run.
- Add the Pass-5-replay regression test to the integration suite (manual-only, tag `[s6.17]`).
- Update CLAUDE.md "Current Stage" line + relevant Code Map rows in the same commit that lands Task A and Task B's first PR.
