# S6.10 -- Small-Model Robustness Pass

## Problem

Locus already covers the structural gaps in coding-agent design: multi-decoder stream parsing (S4.N), checkpoint/undo (S4.B), plan mode (S4.D), MCP (S4.G), memory bank (S4.R), per-turn file-change awareness (S4.T), per-model presets (S4.V Task 6). What it does NOT yet handle is eight concrete failure modes that surface when the model is a local 7-26B (Gemma E4B, Qwen3-Coder-7B, Qwen3.6-35B-A3B, DeepSeek-R1 distills) instead of a frontier API:

1. **Malformed JSON in tool-call bodies.** Small models emit `{"path":"foo.cpp",}` (trailing comma), `{path: "foo.cpp"}` (unquoted key), or close-brace-counts-off output. Current decoder path: try `nlohmann::json::parse`, on failure log a warn, drop the call. The model then says "let me try again" and burns a round on the same content. SmallCode and little-coder both pay this cost up-front with a repair pre-pass and get the call through.

2. **Stalled / looping / hallucinated tool calls.** The same tool with the same args fires three turns in a row. The model emits a tool name that doesn't exist. The assistant response is empty (no text, no tool call). Locus has the *cap-and-bail* side covered already (`max_rounds_per_message` default 100; S6.13's `nudges_this_turn_` 2-cap + "Agent appears stuck" abort; ToolDispatcher try/catch fence) so the agent thread can no longer pin or crash. What's still missing is *recovery*: the model gets bailed out but never sees a targeted corrective message describing what it actually did wrong. little-coder's `quality-monitor` catches these four cases at `tool_result` and injects a specific corrective message into the next turn -- no re-prompt from scratch.

3. **Thinking blocks pile up across history.** Reasoning models (Qwen3-Thinking, DeepSeek-R1-Distill, GLM-Z1) emit 5-20k thinking tokens per turn. Locus already routes them to `on_reasoning` (separate channel from `on_text`) but `ConversationHistory` stores them and ships them back to the LLM next turn as part of the assistant message. After 3-4 thinking-heavy turns the prefix cache is shot and the next request blows the context window. Past thinking is a scratchpad for the decision that was already made -- it adds no information once the tool result is back in the conversation.

4. **Server-side grammar-constrained decoding is unused.** llama.cpp accepts a GBNF grammar; LM Studio and vLLM accept `response_format: { type: "json_schema", ... }`. With the grammar set, the token sampler can only emit tokens that keep the output parseable -- malformed JSON tool calls become physically impossible to generate. Locus currently relies entirely on post-hoc parsing + (with Task A) post-hoc repair. The grammar path is strictly better where the server supports it.

5. **Tool descriptions lack examples.** Small models follow tool schemas literally. Locus's `description()` strings today are prose explanations of the parameters with no worked examples. Frontier models gloss over the difference; local 7-26B does not. Concrete loss: `search` with five modes (text / regex / symbols / semantic / hybrid) gets the wrong mode picked routinely; `edit_file` with the batch-of-edits shape (`edits: [{oldText, newText}, ...]`) confuses models that haven't seen that exact shape in training; `run_command_bg` -> `read_process_output` handoff is opaque.

6. **Wrong preset silently applied across model swaps.** Users running multiple models against the same LM Studio instance (swap Qwen-Coder for Gemma between sessions) must remember to re-pick the `tool_format` and chat-template preset in Settings. Today the preset doesn't auto-sync to the actually-loaded model. Wrong combination drops tool-call success by 30-50%.

7. **Code writes can be silently truncated.** Small models stop mid-file at line 140 of 200, emit a `// ... rest of the function` comment or a literal `...` and end the message. `WriteFileTool` writes the truncated content; the user has to notice the file is broken. No detector exists for trailing-ellipsis / unbalanced-brace / "TODO: implement" patterns.

8. **Sampler knobs default to server-side defaults that are often wrong.** `ModelPreset` (S4.V Task 6) carries `temperature` / `max_tokens` / `tool_format` but not `top_k` / `top_p` / `repetition_penalty` / `min_p`. The Settings UI exposes these (S4.V Task 5) at the workspace level but per-model defaults don't capture them. Real cases: Qwen3-Coder wants `temp=0.1, top_p=0.9`; Gemma 4 wants `temp=0.7, top_k=64`; DeepSeek-R1-Distill wants `temp=0.6, top_p=0.95`.

All eight target the same target user: someone running a 7-26B model on consumer hardware. None of them touch the byte-stable system prompt invariant (S4.F).

## Lands in M6 because

- M5 finished the polish pass; the engine is now stable enough that turn-level recovery behaviour is the next observable quality lever.
- All three changes hook into surfaces that exist today: stream decoder JSON parse points, `ToolDispatcher` between-round seam, `LLMContext` payload assembly. No new subsystem.
- Independent of S6.0-S6.9 connectivity work; can land before or after.

## Tasks

### Task A -- JSON repair pre-pass for tool-call bodies -- DONE 2026-05-25

- [x] New helper `src/llm/json_repair.h/cpp`: `std::optional<JsonRepairResult> repair_for_parse(std::string_view)`. Stages run in order, each tracked in a pipe-separated `stages_applied` diagnostic that goes to the trace log:
  - `trailing_comma` -- strip `,` before `}` or `]` (string-state-aware)
  - `unquoted_keys` -- wrap bare identifier keys: `{path: "x"}` -> `{"path": "x"}`
  - `single_quotes` -- convert `'foo'` -> `"foo"` (only when no `"` in input)
  - `literal_controls` -- escape raw `\n` / `\r` / `\t` inside string values
  - `balance` -- count opens vs closes, append matching closers; closes an unterminated string with `"` first so closers land outside
  - `extract_balanced` (fallback) -- pull the first balanced `{...}` block out of a longer string, then re-run stages 1-5 on the trimmed window
- [x] `XmlToolCallExtractor::parse_qwen_body` retries via `repair_for_parse` on parse failure; both Qwen and Auto / Claude XML decoders inherit. Logged at trace with the stages list.
- [x] `ToolRegistry::parse_tool_call` runs the pre-pass before falling back to an empty args object. Trace log carries the stages list per parse.
- [x] Field-name aliases at the arg-binding layer: when the outer object has exactly one key matching `parameters` / `args` / `arguments` / `input` AND its value is an object, the wrapper is lifted so downstream tools see the canonical flat shape. Negative-case guard: don't hijack a literal `args` parameter on a tool that legitimately takes one.
- [x] Catch2 tests `tests/test_json_repair.cpp` `[s6.10][json_repair]`: 13 cases covering each stage, clean-input fast path, combined-error case, end-to-end through `XmlToolCallExtractor`, and pathological-input nullopt returns.
- [x] Coverage for the dispatcher-side integration also lives in `tests/test_tools.cpp` `[s6.10][tool_registry]` -- exercises `parse_tool_call` with trailing-comma / unquoted-keys / surrounding-prose + missing-brace inputs and asserts the resulting `ToolCall.args` is the canonical shape; plus the field-name-alias lift + negative cases.
- [x] Integration tests `tests/integration/test_int_tool_calls.cpp`:
  - `[integration][s6.10][json_repair]` -- deterministic case that drives `ToolRegistry::parse_tool_call` against the harness's live workspace with three malformed inputs (trailing comma + unquoted keys, prose + missing closer, `arguments` wrapper). Verifies the repair pipeline is wired through the production factory path.
  - `[integration][llm][s6.10][json_repair]` -- live regression: runs a normal `read_file` tool-call turn end-to-end, asserts no errors and the tool was called. Guards against the Task A catch-block breaking the happy path.

### Task B -- Quality monitor (turn-level auto-correction) -- DONE 2026-05-25

S6.13 already shipped the **infrastructure** Task B was going to build: `AgentCore::nudges_this_turn_` counter with a hard cap of 2, "Agent appears stuck" abort on the 3rd would-fire, `request_commit_now()` as the programmatic injection entry, and the synthetic-user-message pattern. Task B is now strictly about the **detector layer** -- read the latest assistant message + recent tool-result history, decide a corrective is warranted, and call the existing nudge entry point with a detector-specific message body.

**Scope reduced from the original 4-detector spec.** Task A's repair pre-pass + the ToolDispatcher try/catch fence (both shipped 2026-05-25) already handle `malformed_args` end-to-end -- the corrective message "provide a proper JSON object" tells the model what it already failed to do, and the dispatcher already returns `ToolResult{false, "[tool failed]..."}` which the model sees as feedback. `unknown_tool` is **partially redundant**: the dispatcher already errors on unknown names, so the only value-add is appending a closest-match suggestion + visible-tools list -- that's a ~10-line tweak to `ToolDispatcher`'s existing unknown-tool path, not a new detector layer. The two genuinely-new detectors below are `empty_response` and `repeated_tool_call`.

- [x] New class `src/agent/quality_monitor.h/cpp`. Stateless helpers that inspect the latest assistant message + tool-call history; returns an optional `QualityCorrection { Kind, std::string corrective_message }`. No counter, no cap, no abort path -- those live on `AgentCore` already.
- [x] Two detectors:
  - **`empty_response`**: assistant message has no `content`, no `tool_calls`, no surfaced reasoning that became text. Corrective: "Your previous response was empty. Respond with either text or a tool call."
  - **`repeated_tool_call`**: same tool name + canonical-JSON-stringified args as the previous tool call (or two consecutive). Corrective: "You just called `<name>` with the same arguments and it didn't produce new information. Try a different approach -- different arguments, a different tool, or a question."
- [x] Companion fix in `ToolDispatcher` (NOT a detector): when the model emits a tool call with a name not in the visible manifest, the dispatcher's existing unknown-tool error message gains a closest-match suggestion (lift `closest_name` from `describe_tool.cpp`'s unnamed namespace into `tools/shared.h` -- shipped as `tools::closest_tool_name`) + a short list of the tools that WERE in scope this round. Single trace + warn log, single error message; no new detector, no new schema. Functionally replaces the spec's `unknown_tool` quality-monitor detector with less plumbing.
- [x] `malformed_args` detector intentionally **NOT included**. Task A's repair pre-pass + the dispatcher try/catch fence already cover the failure mode end-to-end. If Task A's repair gives up (returns `nullopt`), the dispatcher already returns `ToolResult{false, ...}` to the model -- adding a "provide a proper JSON object" corrective message on top doesn't help the model do what it already failed to do.
- [x] Hook in `AgentCore`: after `ToolDispatcher` returns and before the next round begins, run the monitor. On a fired correction, route it through the same `nudges_this_turn_` + synthetic-user-message path S6.13 uses (factored out as `AgentCore::inject_nudge(std::string body, const std::string& reason_tag)` -- shared with the watchdog path). The 2-cap and "Agent appears stuck" abort apply automatically.
- [x] Settings flag: `WorkspaceConfig::quality_monitor_enabled` (default `true`). Config round-trip lands in `agent.quality_monitor_enabled`. Settings UI checkbox follow-up tracked separately.
- [x] Activity log: each fired correction emits a `quality_correction { kind, summary }` event so the user sees what happened (and why the model "magically improved" mid-turn).
- [x] Catch2 tests `tests/test_quality_monitor.cpp` `[s6.10][quality]`: 10 cases covering empty / healthy / reasoning-only-not-empty / repeated_tool_call / different-name / different-args / order-insensitive-args / first-call / non-tool intervening, plus the dispatcher unknown-tool closest-match path indirectly via the live integration test.

**Deferred to a polish pass:**
- ~~Chat-footer chip `auto-corrections: N` (per session, click to filter activity log to the `quality_correction` events).~~ **Moved to [S6.18](S6.18-index-storage-hygiene.md) Task C.4** (2026-05-27) -- folds into the GUI-completions bucket alongside the compacted-chip update.

### Task C -- Strip past-turn thinking from LLM payload -- DONE 2026-05-25

- [x] `ChatMessage::reasoning_content` field added (S4.N split the stream decoder channels but didn't persist the reasoning channel into `ChatMessage`; this task captures it). `to_json` / `from_json` round-trip the field via the wire-format key `reasoning_content` (matches LM Studio's payload key). `AgentLoop` writes `out.assistant_msg.reasoning_content = accumulated_reasoning` so the just-finished turn's reasoning is available to the immediate next round.
- [x] `AgentLoop::run_step` builds an `outgoing = history.messages()` copy right before the `llm_.stream_completion` call and strips `reasoning_content` from every assistant message EXCEPT the most recent when the very last message in history is a tool result (mid-decision-chain). Other rounds strip every past-turn reasoning. Logged at trace as `stripped N bytes of past-turn reasoning from outgoing payload`.
- [x] Edge case handled: assistant messages with `content.empty() && tool_calls.empty()` (only-reasoning) skip the strip. One-line check, no sentence heuristic, per the scope note.
- [x] Display / activity / log unchanged. The strip is payload-only -- the chat panel + activity log read from `ConversationHistory`, which retains the original `reasoning_content`.
- [x] Saved session JSON keeps full reasoning. `ConversationHistory::to_json` is unchanged structurally -- the per-message `to_json` now emits `reasoning_content` when present, so saved+loaded session round-trips. The strip fires fresh at payload-build time.
- [x] Token-cost log at trace level: `AgentLoop: stripped N bytes of past-turn reasoning from outgoing payload` -- one line per round, only fires when N > 0.
- [x] Feature flag `WorkspaceConfig::strip_past_thinking` (default `true`) round-trips through `.locus/config.json` as `agent.strip_past_thinking`. Disabling the flag short-circuits the build-outgoing branch so the LLM sees the full history.
- [x] Catch2 tests `tests/test_thinking_strip.cpp` `[s6.10][thinking_strip]`: 8 cases covering ChatMessage / ConversationHistory round-trip, 3-turn strip, mid-decision-chain preservation, only-reasoning preservation, feature-flag off, and system / user / tool message immunity.

### Task D -- Server-side grammar-constrained decoding for tool calls -- DONE 2026-05-25 (response_format path; GBNF + server probe deferred)

- [x] `GrammarMode { Off, BestEffort, Strict }` enum + `to_string` / `grammar_mode_from_string` converters in `src/llm/llm_client.h/cpp`.
- [x] `LLMConfig::grammar_mode` field (default `Off`) + `ModelPreset::grammar_mode` field. LM Studio / llama-server presets default to `BestEffort`; Ollama and the non-tool-trained preset stay `Off`.
- [x] `WorkspaceConfig::llm_grammar_mode` (string, default `"off"`) round-trips through `.locus/config.json` as `llm.grammar_mode`. `LocusSession::load_shared_resources` layers the string over the seed config via `grammar_mode_from_string`.
- [x] Helper at `src/llm/tool_call_grammar.h/cpp`:
  - `build_tool_call_union_schema(tools)` produces the OpenAI / LM Studio Structured Output wrapper -- `{name, strict: true, schema: {properties: {tool_calls: {type:array, minItems:1, items: oneOf[<per-tool variant>]}}}}`. Single-tool fast path skips the `oneOf`. Empty params get a permissive object schema.
  - `grammar_should_attach(mode, tool_format, tool_count)` gates the attach -- `Off`, `ToolFormat::None`, or zero tools means skip.
- [x] `LMStudioClient::build_request_body` attaches `response_format: {type: "json_schema", json_schema: <wrapper>}` when `grammar_should_attach` is true. `LMStudioClient::stream_completion` wraps the request with a BestEffort retry intercept: when the server returns a grammar-incompatibility error (LM Studio: `"Cannot combine structured output constraints with lazy grammar"`; matched on substrings `"structured output"` / `"lazy grammar"` / `"response_format"` / `"json_schema"`), the error is captured, a session-wide `grammar_disabled_for_session_` atomic is set, and the request is retried once with `response_format` skipped. Strict mode propagates the error without retrying. The error happens before any content streams in practice, so user callbacks don't see duplicate output. Task A's repair pre-pass remains the universal fallback for the post-retry unconstrained path.
- [x] Settings UI: `Grammar mode` dropdown on the LLM tab next to `Tool-call format`. Three values (`Off` / `Best effort` / `Strict`); tooltip explains the response_format mechanism, the server-honour caveat, and that Task A still runs as fallback. Wired through `LlmSettingsPanel::load_from_config` + `commit_to_config` + the preset apply path. Widget name `locus.settings.llm.grammar_mode` for UIA.
- [x] Activity log: per-session `LLM client: ... grammar_mode=X` info line on construction; per-request `grammar_mode=X response_format attached` trace when the attach fires.
- [x] Catch2 tests `tests/test_tool_call_grammar.cpp` `[s6.10][grammar]`: 7 cases covering GrammarMode <-> string round-trip, single-tool fast path, multi-tool `oneOf` union, empty-input null return, empty-params permissive object, the gate matrix, and the preset round-trip.
- [x] Integration test `tests/integration/test_int_tool_calls.cpp` `[integration][llm][s6.10][grammar]`: builds a separate `LMStudioClient` with `grammar_mode=BestEffort` against the harness's endpoint + model, hands it the real built-in tool manifest, and asserts a clean tool call comes back (well-formed `name` + parseable `arguments`). Live verified on `google/gemma-4-e4b` (2026-05-25): LM Studio rejects the combined `response_format` + `tools` shape; the BestEffort intercept fires, sets `grammar_disabled_for_session_`, and the retry without `response_format` succeeds -- the read_file tool call comes back with valid JSON args.
- [ ] **Deferred to a follow-up**:
  - Live server probe (`/props` for llama.cpp, tiny response_format probe for LM Studio / vLLM) so `Strict` can actually refuse-on-unsupported instead of treating like `BestEffort`. Today Strict is functionally equivalent to BestEffort -- the dropdown is in place so the future probe can light up without a UI change.
  - GBNF grammar (`src/llm/tool_call_grammar.h/cpp`) for the Qwen-XML / Claude-XML formats -- llama.cpp's `grammar` field constrains the entire output stream including XML-marker bodies. `response_format` only constrains the JSON `content` slot, so it covers the OpenAI-native tool_format path but not the XML paths. A follow-up will add `build_tool_call_gbnf(tools, format)` + the request-body wiring.
  - `[integration][llm][s6.10]` integration test against a model known to emit malformed JSON in unconstrained mode -- punted because it requires a curated known-bad model loaded in LM Studio and the unit tests already cover the schema-generation + gate logic.

### Task E -- Few-shot examples in built-in tool descriptions -- DONE 2026-05-25

- [x] Audited every built-in tool's `description()` string in `src/tools/`.
- [x] Pattern: where it pays for itself, the description ends with one `Example:` block showing a canonical call (deliberate, minimal, ASCII-safe).
- [x] Tools with the highest leverage -- examples added:
  - `search` (`src/tools/search_tools.cpp`): one example per mode (text / regex / symbols / ast / semantic / hybrid). Rendered via `SearchTool::description_for(ws)` so only modes available in the workspace appear.
  - `edit_file` (`src/tools/file_tools.cpp`): two examples -- single edit, batch of two edits. Highlights `edits` array shape.
  - `write_file`: one example with `overwrite: false` (default).
  - `run_command_bg` + `read_process_output` + `stop_process`: one chained example showing the id handoff (on `run_command_bg`).
  - `propose_plan` (plan mode): one example with 3 steps + `tools_needed`.
  - `add_memory` + `search_memory`: one example each.
  - `read_file`, `list_directory`, `get_file_outline`: one example each.
  - `ask_user`: one example (the current schema doesn't carry `options`; revisit if added).
- [x] Tools where an example is **not required**: zero-param tools (`list_processes`, `mark_step_done`, `describe_tool`, `delete_file`, `filter_output`) -- skipped intentionally.
- [x] MCP tools out of scope -- their `inputSchema` comes from the server.
- [x] Token impact: examples ride along in the full description only. `short_description()` (Task I) keeps the lazy-manifest path lean; under `lazy_manifest && (tool_format == OpenAi || Auto)` (Task J) the per-turn manifest skips the prose section entirely. Net cost in lazy+openai_json mode is effectively zero; the examples are visible to the model only when it calls `describe_tool('<name>')`.
- [x] No Catch2 lint test forcing the `Example:` substring on every tool -- discipline + per-tool review is the right enforcement level (a build-time gate would force noise on zero-param tools).

### Task F -- Auto-detect loaded model and auto-apply preset -- DONE 2026-05-25

**Scope reduced from the original spec.** Original called for a startup detect + a mid-session re-detect on model swap + `last_applied_preset` persistence in `.locus/ui_state.json` + a chat-footer chip / one-time toast + an extra 500ms timeout layer around `query_model_info`. Most of that is unnecessary: users who swap models in LM Studio mid-session generally know they're doing it and can manually re-pick the preset; the startup-time detect is 95% of the value. `query_model_info` already runs at startup with its own 5s timeout -- adding a second layer is duplicate plumbing. A startup info-log line is enough surface; the chip / toast is a polish item that adds visual clutter for a one-time event.

- [x] At `LocusSession::load_shared_resources` (after the LLM client is built and `query_model_info()` has run), reuse the existing model_info. No extra probe.
- [x] New matcher `find_preset_for_model(std::string model_id) -> const ModelPreset*` in `src/llm/model_presets.cpp`. Case-insensitive regex match on the lower-cased model id (publisher/ prefix stripped). Order: `qwen.?3.*coder` -> Qwen3-Coder preset, `deepseek.*r1` -> DeepSeek-R1-Distill preset, `qwen.?(2.5|3)` -> Qwen family, `gemma.?(2|3|4)` -> Gemma family, `(llama.?3|meta.*llama.?3)` -> Llama 3.x. Returns nullptr on no match.
- [x] Behaviour: `WorkspaceConfig::auto_detect_model_preset` (default `true`) gates the path. When on, the `llm_preset_name` field is consulted first -- a literal preset name pins it, `""` / `"auto"` delegates to the matcher. Each branch logs at info: `applied user-pinned preset '<name>'` or `auto-applied preset '<name>' for model '<id>'` or `no preset matched model id '<id>'; keeping current config`. No chip / toast.
- [x] Settings tab adds the auto-detect checkbox `locus.settings.llm.auto_detect_preset`, round-tripped through `LlmSettingsPanel::load_from_config` / `commit_to_config`.
- [x] No mid-session re-detect, no `last_applied_preset` cache (deferred per the scope note).
- [x] Catch2 tests in `tests/test_model_presets.cpp` `[s6.10][presets][matcher]`: empty / unknown / Qwen3-Coder / generic-Qwen / Gemma 2/3/4 + publisher prefix / DeepSeek-R1 distills / Llama 3.x variants. Live integration test in `tests/integration/test_int_small_model_robustness.cpp` `[integration][s6.10][presets][matcher]` exercises the matcher against the harness's currently-loaded model.

**Deferred to a follow-up** (low value, high complexity):
- Mid-session model-swap re-detect + `last_applied_preset` persistence.
- Chat-footer chip / one-time toast for the auto-apply event.

### Task G -- Anti-truncation detection for code writes -- DONE 2026-05-25

**Scope reduced from the original spec.** The full version (Tree-sitter ERROR-node check + trailing-`...` regex + Python `pass` heuristic + literal phrase list) carries a real false-positive cost in real code -- `...` collides with Python's `Ellipsis` literal / JS spread / C++ varargs; Tree-sitter ERROR collides with WIP code the model is intentionally drafting mid-edit; `def foo(): pass` collides with legitimate stubs and abstract methods. The literal phrase list, by contrast, is purely diagnostic: no legitimate code anywhere contains `// rest of the implementation` or `// ... existing code ...`. Ship the high-precision slice first; expand only if the FPR on real workloads tells us we're missing failure modes the phrase list doesn't catch.

- [x] New detector in `src/tools/truncation_detector.h/cpp`. Pure function `std::optional<std::string> detect_truncation_phrase(content)` returns the matched phrase when found, `nullopt` otherwise. Pure CPU, no FS / parser dependencies -- the entire detector is a substring scan over the last ~200 chars of the input.
- [x] Phrase list (language-agnostic, case-insensitive substring match on last 200 chars of `content`; each phrase is a small-model-specific elision marker that no real code contains):
  - `// rest of the code`
  - `// rest of the implementation`
  - `// ... existing code ...`
  - `// (continued)`
  - `// TODO: implement` (when at EOF, not mid-file -- legit TODOs live anywhere)
  - `# rest of the code`
  - `# rest of the implementation`
  - `# (continued)`
  - `# TODO: implement` (same EOF guard)
  - `/* ... rest ... */`
  - `<!-- rest of the code -->` (HTML / XML)
- [x] Hook in `WriteFileTool` (full content) AND `EditFileTool` (each `new_string` in the edits array): after content is assembled but BEFORE `write_atomic` fires, the detector runs. On detection, returns `ToolResult{success=false, content=...}` whose message names the matched phrase plus the override hint pointing at `agent.detect_write_truncation`. The ToolResult also carries `activity_tag="truncation_blocked"` (new optional field) so the dispatcher emits a companion `ActivityKind::truncation_blocked` event.
- [x] Hard-error refusal is the default -- false positive = cheap re-issue; false negative = silent corruption.
- [x] Settings flag: `WorkspaceConfig::detect_write_truncation` (default `true`) round-trips through `.locus/config.json` as `agent.detect_write_truncation`.
- [x] Activity log: dispatcher fans out a `truncation_blocked` event when the tool result's `activity_tag == "truncation_blocked"`. The event detail carries the tool name, path, and matched phrase. Same plumbing also routes `quality_correction` events (Task B).
- [x] Catch2 tests `tests/test_truncation_detector.cpp` `[s6.10][truncation]`: 15 cases covering empty / clean code / bare `...` (Python / JS / C++) / `// rest of the code` / `// rest of the implementation` / `// ... existing code ...` / `// (continued)` / Python `# ...` variants / `/* ... rest ... */` / HTML `<!-- rest of the code -->` / case-insensitivity / trailing-whitespace handling / mid-vs-EOF TODO distinction / 200-char-tail window.
- [x] Integration: `WriteFileTool` and `EditFileTool` integration tests in `tests/test_tools.cpp` `[s6.10][truncation]` + `tests/integration/test_int_small_model_robustness.cpp` `[integration][s6.10][truncation]` -- both assert refusal, the `activity_tag`, the matched phrase in the result, the file not being created (write) / not being modified (edit), and the disable-via-config path.

**Skipped intentionally** (2026-05-27): the phrase-list approach has been live since 2026-05-25 with no observed misses on real workloads, and the three extensions below all carry a real false-positive cost that the phrase list avoids by construction. Re-evaluate only if a concrete failure mode surfaces that the phrase list cannot reach.
- Tree-sitter ERROR-node + brace-balance check -- heavy dependency on the tool layer; false-positive risk on intentionally partial code (WIP edits the model is drafting mid-turn); reusing the json_repair `patch_balance` walker would be the cheap fallback if we ever do want it.
- Trailing-`...` / `// ...` / `# ...` / `/* ... */` regex check -- false-positive risk on language idioms (Python `Ellipsis`, JS spread, C++ varargs).
- Python `def foo(): pass` heuristic -- false-positive risk on legitimate stubs / abstract methods; language-specific complexity.

### Task H -- Per-model sampler defaults (top_k / top_p / repetition_penalty / min_p) -- DONE 2026-05-25

**Mostly already done by S4.V Task 7.** `ModelPreset` already carries `top_p`, `top_k`, `min_p`, `repeat_penalty` fields ([src/llm/model_presets.h](../../src/llm/model_presets.h)); `LLMConfig` already propagates them through `LMStudioClient::build_request_body` with the 0-sentinel "don't send" semantics; the Qwen preset already populates `top_p=0.8, top_k=20, min_p=0.05`; the Llama 3.x preset already populates `repeat_penalty=1.1`. Task H's remaining scope is configuration tuning + a couple of new family-specific presets + the "Reset to preset defaults" affordance.

- [x] (S4.V Task 7) Extend `ModelPreset` with `top_p` / `top_k` / `min_p` / `repeat_penalty`; propagate through `LLMConfig` -> `OpenAiTransport` -> request body.
- [x] Added `LM Studio -- Qwen3-Coder` preset (`temp=0.1`, `top_p=0.9`, `tool_format=Auto`).
- [x] Added `LM Studio -- DeepSeek-R1-Distill` preset (`temp=0.6`, `top_p=0.95`).
- [x] Tuned `LM Studio -- Llama 3.x family` to add `top_p=0.9` (alongside existing `repeat_penalty=1.1`).
- [x] Tuned `LM Studio -- Gemma family` to add `top_k=64`.
- [x] Settings UI -- new **"Reset samplers to preset"** button (`locus.settings.llm.sampler_reset_btn`) next to "Reset to global defaults". Resets the sampler block only (top_p / top_k / min_p / repeat_penalty / frequency_penalty / presence_penalty); temperature / max_tokens / tool_format are left intact so a user who has hand-tuned temperature can re-baseline samplers without losing it. When "Custom" is selected the button zeros the sampler block (matching the implicit Custom behaviour).
- [x] Catch2 tests `tests/test_model_presets.cpp` `[s6.10][presets][samplers]`: Qwen3-Coder + DeepSeek-R1-Distill row existence, Gemma top_k=64, Llama 3.x top_p=0.9. The existing "presets without overrides stay at 0" guard was scoped to Ollama / llama-server / non-tool-trained -- Gemma / Llama 3 / Qwen3-Coder / DeepSeek now carry tuned values.

### Task I -- `ITool::short_description()` for lazy-manifest mode (S6.11 follow-up) -- DONE 2026-05-25

Live verification of [S6.11](S6.11-lazy-tool-manifest.md) on `google/gemma-4-e4b` against TestLocalVibe (2026-05-25) showed the lazy manifest delivered ~40% of the predicted token save, not the ~85% the ADR estimated. Root cause: lazy mode strips per-param schemas but keeps tool **descriptions** intact -- and the descriptions are the heavy bit. `SearchTool` alone covers 5 modes with examples (~400+ chars); `run_command` / `run_command_bg` / `read_process_output` each carry detailed prose; `edit_file` has the multi-edit-batch explanation. Combined, descriptions for 13 tools dominate the remaining 1753 t lazy manifest.

- [x] Add `virtual std::string short_description() const { return description(); }` to `ITool` (`src/tools/tool.h`). Default forwards to the existing description so non-override tools are unchanged.
- [x] `ToolRegistry::build_entry(...)` and `SystemPromptAssembly::build(..., lazy_manifest=true)` switch to `short_description()` when `lazy=true`. Full mode keeps `description()` unchanged. `describe_tool` itself keeps its real description in both modes (the model needs the entry point spelled out).
- [x] Override `short_description()` on every built-in tool with a curated <60-char one-liner. Final overrides ship on all 18 registered built-ins (`read_file` / `write_file` / `edit_file` / `delete_file` / `list_directory` / `get_file_outline` / `search` / `run_command` / `run_command_bg` / `read_process_output` / `stop_process` / `list_processes` / `ask_user` / `propose_plan` / `mark_step_done` / `add_memory` / `search_memory` / `filter_output`). `describe_tool` intentionally inherits the default (keeps full description in lazy mode).
- [x] Tests: `tests/test_lazy_manifest.cpp` `[s6.10][lazy_manifest][task_i]` covers:
  - `build_schema_json(lazy=true)` description slot carries `short_description()` text; `lazy=false` carries `description()`.
  - Regression guard via a second `FakeToolNoShort` with no override -- proves a tool that skips the override silently gives back the saving.

### Task J -- Drop the prose `## Available Tools` section when `lazy + openai_json` (S6.11 follow-up) -- DONE 2026-05-25

Companion to Task I. For `tool_format=openai_json` (and the openai_json branch of `auto`), the API `tools: [...]` array IS the tool listing the LLM consumes -- the prose `## Available Tools` section in the system prompt is pure duplication, currently costing ~1095 t even with lazy mode on (live measured 2026-05-25). For `qwen_xml` / `claude_xml` the section is load-bearing (those formats don't consume the API tools array), so keep it.

- [x] `SystemPromptAssembly::build(..., tool_format, ..., lazy_manifest, ...)` uses both inputs. When `lazy_manifest=true` AND `tool_format == ToolFormat::OpenAi` (or `ToolFormat::Auto`), the section degrades to a one-line `## Tools\nSee the API tools[] array... Call describe_tool('<name>')...` pointer so the model still has the entry-point name visible.
- [x] `Auto` format treated like `OpenAi` -- the auto decoder runs all three branches in parallel, but the openai_json path is the cheap one and the XML fallbacks degrade gracefully on a missing prose section (the LLM also sees the API array).
- [x] `tests/test_lazy_manifest.cpp` `[s6.10][lazy_manifest][task_j]` asserts the four-cell matrix: prose absent for `(lazy, OpenAi)` and `(lazy, Auto)`; prose present with short-description bullets for `(lazy, Qwen)` and `(lazy, Claude)`. Hashes differ across the visible-section variants.

**Combined Task I + J -- live measurement 2026-05-25 (post-implementation)**

Synthetic budget table from `[.s6_10_budget]` in [tests/test_manifest_budget.cpp](../../tests/test_manifest_budget.cpp), measured against WS1 (the Locus repo itself, ~12 built-in tools active, no MCP). FakeWorkspaceServices means `describe_tool` is filtered out -- real-workspace lazy columns would land ~120-180 t higher.

```
config              sys_total    sys_prose   api_tokens  api_bytes
------------------------------------------------------------------
full + auto              2785         2067         2555      10218
lazy + qwen               996          211          505       2020
lazy + claude            1017          211          505       2020
lazy + openai             762           44          505       2020
lazy + auto               762           44          505       2020
```

**Headline saving** -- per-turn fixed overhead (system prompt + API tools array combined):

| config                       |  total | vs full baseline |
|------------------------------|-------:|-----------------:|
| full + auto (baseline)       | 5340 t | --               |
| lazy + qwen   (Task I only)  | 1501 t | **-72%**         |
| lazy + openai (Task I + J)   | 1267 t | **-76%**         |

**Per-task contribution**:

- **Task I** (short_description swap) collapses the prose tool section from **2067 t -> 211 t** (-90%). This is the half-of-the-manifest the original 2026-05-25 measurement said was still on the table after S6.11 alone.
- **Task J** (skip prose section entirely on openai_json / auto) drops the section from **211 t -> 44 t** (-79% on top of Task I). The 44 t remainder is just the section header + the one-line pointer at the API `tools[]` array + the `describe_tool('<name>')` hint.
- API tools array bytes / tokens are unchanged across all lazy combos. S6.11 collapsed the `parameters` schemas already; Task I shrinks the `description` slot too -- the lazy column shows **505 t / 2020 B versus the full-mode 2555 t / 10218 B**, a -80% reduction from S6.11 + Task I combined.

**Versus ADR-0007 projection** -- the ADR called for ~85% savings on combined manifest + prompt. Delivered **-76% on WS1**. Per-workspace number scales with active tool count (TestLocalVibe's prior measurement had a larger absolute baseline because more tools surfaced there) but the percentage shape carries.

The `[.s6_10_budget]` test stays in the suite as a regression check. Tag-prefixed with `.` so it does not run in the default sweep; invoke explicitly with `locus_tests.exe "[.s6_10_budget]" --reporter compact` to print the table.

## Dependencies

- None blocking. Lands when scheduled. Independent of the connectivity stages (S6.0-S6.6) and the storage migration (S6.8).
- Internal task order: A and D are complementary (D obsoletes A's repair pipeline where servers support it; A is the universal fallback). Ship A first as the safety net, then D as the optimization (both done). G is now the cheapest remaining task (~50 LOC phrase-list scan; see Task G's scope note) and prevents a catastrophic failure mode (silently committed truncated files) -- ship before B. G's `truncation_blocked` event surfaces through B's quality-correction display path when B lands. C is independent of all others. E, F, and H are independent of each other and of A-D.
- Task B builds on [S6.13 (Reasoning watchdog + auto-nudge)](S6.13-reasoning-watchdog.md) which is already landed -- specifically `AgentCore::nudges_this_turn_`, the `request_commit_now()` entry point, and the synthetic-user-message injection pattern. Task B is the detector layer over that infrastructure.
- Tasks I and J build on [S6.11 (Lazy Tool Manifest)](S6.11-lazy-tool-manifest.md) which is already landed. They are independent of A-H; either can land first. Task I (short_description) recoups ~half the remaining manifest budget; Task J adds another half on top for openai_json setups. Combined they close the gap between the ADR-0007 projection and the measured outcome.

## Implementation notes

- Task A is reused by all three XML decoder variants (Qwen / Claude / Auto) plus the OpenAI-native path. Keep the helper in `src/llm/` with no decoder-specific includes so all four call it.
- Task B's correction messages are templated strings, not LLM-generated. Keep them deterministic -- this is feedback, not summarization.
- Task C is the most measurable win. On a Qwen3-Thinking 14B running locally, a single thinking-heavy turn can push 8k tokens into history. Three turns and the prefix cache no longer fits. The strip is the cheapest fix in the stage by far.
- Task D's grammar source is the same JSON schema Locus already builds for the OpenAI-native `tools: [...]` array. The work is wiring it into a second header (`response_format` / `grammar`), not authoring schemas from scratch.
- Task E enforcement is a build-time lint, not runtime. If the assertion fires, the build fails -- no risk of shipping a tool without an example by accident.
- Task F's matcher is conservative: unknown model -> no preset change, never an error. The detection is best-effort signalling, not authoritative configuration.
- Task G reuses the `TreeSitterRegistry` already linked for the indexer; no new parser dependency. The placeholder-comment regex pass is language-agnostic and runs even on languages without a grammar.
- Task H values come from each model's published model card. Treat them as starting points, not gospel -- the Settings UI exposes overrides anyway.
- All eight tasks ship behind feature flags so a regression in any one can be disabled without disabling the others. Default-on for A / C / E / F / G / H (low-risk); B and D default-on but settings-toggleable.
- No new dependencies. Pure C++ + Catch2. The grammar generation in Task D produces strings handed to the existing transport layer; no new HTTP client work.

## Open questions

- Should Task A's repair stages run in a fixed order or be tried independently and the cheapest fix that parses cleanly wins? Fixed-order is simpler and matches what SmallCode's parser does; consider only changing if test cases surface a conflict between stages.
- Should Task B's `repeated_tool_call` detector look back 1 turn (strict) or 2-3 turns (catches alternating-pair loops like `read_file A -> read_file B -> read_file A -> ...`)? Start with 1, widen if telemetry shows alternating loops.
- Should Task C's "only-reasoning" edge case retain head+tail sentences, or a fixed token cap, or both? Sentence-based is more readable but token-based is more predictable. Default to sentences with a fallback token cap of e.g. 200.
- Should Task D's `best_effort` mode log a one-time warn when the server doesn't support grammar, or stay silent? Lean silent at info, trace-log per request -- otherwise users on older llama.cpp see warns every session.
- Should Task E examples be shown in the model's manifest verbatim, or templated through `LOCUS.md`-style substitution for workspace-specific paths? Verbatim. The whole point is that small models follow exact patterns; substitution that the model can't see breaks the pattern.
- Task F: how long to wait for the model-info probe before falling back to "no preset change"? 500ms hard cap; if the server is slow on `/v1/models` the auto-detect just doesn't fire that session.
- Task G: should `WriteFileTool` refusal be a hard error or a soft warning the user can override? Hard error initially; demote to soft if false-positive rate is high in practice.
- Task H: should sampler values that come from a preset be visible in the Settings UI as "(from preset)" annotations, or just silently pre-fill the fields? Pre-fill with a visible "from preset: <name>" badge under each field.

## Inspiration / reference

- [smallcode](https://github.com/Doorman11991/smallcode) `bin/governor.js` + the "forgiving tool call parser" mechanism (JSON / YAML / XML / Hermes / plain text repair) -- Task A is the JSON slice of this.
- [little-coder](https://github.com/itayinbarr/little-coder) `.pi/extensions/quality-monitor/quality.ts` -- Task B follows the same four detector kinds (empty / unknown_tool / repeated_tool_call / malformed_args) with the same "synthetic corrective message into the next turn" pattern.
- [little-coder](https://github.com/itayinbarr/little-coder) `.pi/extensions/thinking-budget/index.ts` -- Task C is the strictly-better proactive variant of this extension's reactive abort-and-retry model.
- llama.cpp GBNF docs + LM Studio "Structured Output" API + OpenAI `response_format: json_schema` spec -- Task D wires Locus into the constrained-decoding ecosystem that already exists.
- Tool-description conventions from Anthropic Claude tool-use docs and OpenAI function-calling guide (both recommend examples) -- Task E formalizes a discipline most schemas already gesture toward.
- Model-family cards on HuggingFace for Qwen3-Coder, Gemma 4, DeepSeek-R1-Distill, Llama 3.x -- Task H values come from these.
