# S5.R -- Per-Tab Terminal Panel

## Problem

[S5.I](S5.I-tabs-and-sessions.md) made `ProcessRegistry` per-tab (each tab's agent only sees the processes it spawned via `list_processes` / `read_process_output` / `stop_process` / `run_command_bg`) but kept the [S5.B](S5.B-terminal-panel.md) `TerminalPanel` workspace-wide -- every tab's `ProcessRegistry` was given the workspace's shared `ProcessSinkBroker` so all tabs' output funnels into one panel.

The result is asymmetric: the agent in tab A sees a clean per-tab process list, but the user sees tab A's `cmake --build`, tab B's `pytest`, and tab C's dev server interleaved in the same Terminal pane with no way to tell which tab spawned which. Switching tabs in the chat doesn't change what's in the terminal -- the mental model "tab = isolated conversation" breaks at the terminal boundary.

This stage scopes the terminal to the active tab: each tab owns its own terminal content (Run tab + per-bg-process tabs + scrollback + ANSI state), the AUI pane's visibility stays workspace-wide, content swaps on chat-tab activation, and the pane auto-shows only when the **active** tab spawns a command.

## Design

**Per-tab content, workspace-level visibility.** One `TerminalPanel` widget docked once in the AUI layout; its contents (the inner `wxAuiNotebook` of process tabs) are sourced from the active chat tab's terminal state. Switching chat tabs swaps the rendered notebook; the AUI pane's hidden/shown/floated/docked state is unaffected.

**No per-tab toggle button in `ChatPanel`.** The workspace-level **View > Terminal** menu item + Ctrl+` accelerator already exists and is sufficient. Adding a per-tab toggle clutters the chat surface for a workspace-level concern.

**Auto-show on first active-tab command, no auto-hide.** When the **active** tab's `ProcessRegistry` sees the first new process (sync or bg) and the pane is hidden, the pane auto-opens. Subsequent commands in the same tab do not re-open the pane (user may have deliberately closed it). The pane never auto-hides -- failed commands need post-mortem inspection, streaming bg output needs to stay watchable, short commands shouldn't flash open and shut. User closes the pane via View > Terminal / Ctrl+` when done.

**Background-tab commands do not pop the pane.** If tab B is active and tab A's agent spawns a command, the pane stays in whatever state the user left it. The existing tab-title `*` busy badge ([S5.I](S5.I-tabs-and-sessions.md)) already signals "this tab has activity"; the pane only updates when the user navigates to tab A. This keeps the rule "pane opened = I just ran something here" intuitive.

**Tab-switch preserves per-tab state.** Each chat tab carries its terminal state across switches: which Run tab exists, which bg-process tabs exist with their badges (`*` running / `[N]` exit / `[x:N]` killed), scrollback buffers, scroll positions, active sub-tab selection, ANSI parser state. Switching to tab B then back to tab A restores tab A's view exactly.

## Tasks

### Per-tab terminal state model

- [x] Lift the per-process buffers, ANSI parser state, scrollback position, and active sub-tab index off the `TerminalPanel` widget into a new `TerminalPanelState` (struct or POD class) in [src/frontends/gui/terminal_panel_state.h/cpp](../../src/frontends/gui/terminal_panel_state.h). Lives in `locus_core` (no wx dep, same precedent as `AnsiParser`). The widget becomes a view over a `TerminalPanelState*`.
- [x] `LocusTab` ([src/core/locus_tab.h](../../src/core/locus_tab.h)) gains a `TerminalPanelState` member alongside its `ProcessRegistry`. State lives for the lifetime of the tab; destroyed when the tab closes (along with the registry tearing down processes).
- [x] Remove the workspace-shared-broker wiring: each tab's `ProcessRegistry` constructs its own `ProcessSinkBroker`. `TabServices::process_sink()` returns the **tab's** broker so synchronous `RunCommandTool` lands in the active tab's terminal. `Workspace::process_sink()` stays exposed for symbolic completeness; no subscribers in v1.
- [x] Extend `ProcessSinkBroker` ([src/tools/process_sink.h](../../src/tools/process_sink.h)) to support multiple subscribers: replaced `set_sink(IProcessSink*)` with `add_sink(IProcessSink*)` / `remove_sink(IProcessSink*)`. `emit_*` walks the subscriber list under the existing mutex. Per-tab brokers have 1-2 subscribers each in v1 (the tab's `TerminalPanelState` for rendering + `LocusFrame` for auto-show / badge counting).
- [x] Each tab's `TerminalPanelState` subscribes to its own tab's broker as an `IProcessSink` in the tab's ctor; unsubscribes in the tab's dtor (ordered before `ProcessRegistry` teardown so a late chunk on the reader thread can't land on a freed state).

### Active-tab content swap

- [x] `LocusFrame` calls `TerminalPanel::set_state(TerminalPanelState*)` from the existing tab-activation handler (`on_notebook_page_changed`). The widget rebuilds the inner `wxNotebook` from the new state (clear pages, re-add pages from state's process tabs, restore active sub-tab).
- [x] Content swap rebuilds widget pages from the state's stored model. Each `TerminalPanelState` owns per process tab: the parsed `AnsiEvent` log (the styled-line representation), ANSI parser state, and exit-status badge data. On activation the widget creates a `wxStyledTextCtrl` page per tab and replays styled writes from the stored events; on swap-away the widget's STCs are discarded (state's model is canonical -- no buffer copy).
- [x] Fallback if replay latency becomes measurable: cache the rendered STC document per state and swap document handles via `wxStyledTextCtrl::SetDocPointer` instead of replaying. Out of scope unless profiling shows the need.
- [x] Tab-close order: (1) `LocusFrame::teardown_tab_ui` calls `terminal_panel_->set_state(nullptr)` (and re-binds after the active swap), (2) `LocusSession::close_tab` runs, which drops the `LocusTab` and destroys its `ProcessRegistry` and `TerminalPanelState` in member declaration order. `TerminalPanel::set_state(nullptr)` is a legal call (clears all pages, no active state).

### Auto-show behavior

- [x] `LocusFrame` subscribes to each tab's broker as a **second** `IProcessSink` (`TabProcessObserver`, owned per `TabUi`) for the auto-show decision and per-tab bg-process counting. Chunk methods are no-ops; only `on_bg_started` / `on_bg_exited` / `on_sync_started` matter. Rendering is the rendering sink's job (the tab's `TerminalPanelState`).
- [x] Auto-show decision: if event came from the **currently active** tab AND the AUI pane is currently hidden AND the user hasn't manually hidden the pane since the last show, the pane auto-opens. Pure predicate `should_auto_show_terminal(...)` in [terminal_panel_state.h](../../src/frontends/gui/terminal_panel_state.h).
- [x] First-command-per-tab gating: tracked by a per-tab `terminal_user_hidden_` flag on `LocusTab`. Set on AUI pane-close while the tab is active; cleared on manual re-open via View > Terminal.

### Background-tab signaling

- [x] Extend the S5.I `*` tab badge to reflect bg-process state alongside agent-turn state. Added `busy_processes` int to `TabUi` in [src/frontends/gui/locus_frame.h](../../src/frontends/gui/locus_frame.h), incremented in `on_tab_bg_started` and decremented in `on_tab_bg_exited`. `compose_tab_label` shows `*` when `busy || busy_processes > 0`. Sync `run_command` does NOT touch the counter (the agent is already showing busy via `on_turn_start`); only bg processes do.
- [x] If real usage shows users wanting to distinguish "agent thinking" from "bg process running," split into two glyphs later. Out of scope for v1.

### Visibility persistence

- [x] Pane visibility (shown/hidden/floated/docked) stays workspace-level, not per-tab. AUI perspective persistence is still parked (see [S5.B](S5.B-terminal-panel.md) -- defaulted hidden); when that lands as part of [S5.Z](S5.Z-misc-gaps.md) the perspective will capture the terminal pane state alongside the rest. No new perspective machinery in this stage.

### UI automation scripts

- [x] New script [tests/ui_automation/scripts/terminal_per_tab.json](../../tests/ui_automation/scripts/terminal_per_tab.json): spawn a `run_command` in tab A (auto-show fires + assert pane visible), open tab B via Ctrl+T (terminal pane stays addressable -- empty state), Ctrl+Shift+Tab back to A. LLM-dependent (uses a real `run_command` round-trip) -- inherits the existing "fails loudly if LM Studio isn't reachable" pattern. Documented in [tests/ui_automation/README.md](../../tests/ui_automation/README.md).

### Catch2 tests

- [x] `TerminalPanelState` lifecycle (sync flow, bg start/exit, sync_start log reset) -- [tests/test_terminal_per_tab.cpp](../../tests/test_terminal_per_tab.cpp).
- [x] Per-tab broker isolation: register two `TerminalPanelState` instances each subscribed to its own broker; chunks emitted on broker A do not appear in state B.
- [x] Multi-subscriber broker: `add_sink` is idempotent; `add_sink` then emit fires the sink; second `add_sink` then emit fires both; `remove_sink` for one leaves the other receiving; remove the last and emit becomes a no-op.
- [x] Active-tab auto-show predicate: `should_auto_show_terminal(...)` returns true only when event is from active tab AND pane hidden AND user-hid flag is false.
- [x] Snapshot reflects exit_code / killed / timed_out; replay event log clone is independent of state.
- [x] Scrollback cap drops oldest events (per-event-granularity over-trim acknowledged).

## Implementation notes

- The S5.B `IProcessSink` interface ([src/tools/process_sink.h](../../src/tools/process_sink.h)) is unchanged -- per-tab `TerminalPanelState` implements it as the rendering sink, `LocusFrame` implements it as a lifecycle-only observer (no-op chunk methods). The tab's `ProcessSinkBroker` fans events to all its subscribers (the new multi-subscriber API). No broker fan-out across tabs -- each tab has its own broker, isolated.
- The S5.B widget's `wxAuiNotebook` of process tabs is internal; the per-tab state holds an in-memory model of "what tabs exist + their content," and the widget renders it. On swap, the widget destroys its current notebook pages and rebuilds from the new state -- O(small) since process tabs are typically <5 per tab.
- Scrollback storage: each `TerminalPanelState` keeps per-process-tab a parsed `AnsiEvent` log (the styled-line model) trimmed to the 10000-line cap, plus the raw pending-bytes deque the worker thread appends to. On swap the widget rebuilds its `wxStyledTextCtrl` pages by replaying the event log; the state's model is canonical, no buffer copy. Replay is O(line count); 10000 lines is well under 100 ms. Document-handle swap (`wxStyledTextCtrl::SetDocPointer`) is the fallback if profiling shows replay is too slow.
- Threading model: worker threads write to the state via `IProcessSink::on_*_chunk` -- the call takes the state's mutex, appends to the per-process-tab pending-bytes deque, releases, returns. The widget owns a single 33 ms `wxTimer` that drains **only the active state's** pending queues on the UI thread, runs the ANSI parser, appends `AnsiEvent`s to the state's event log, and writes styled bytes to the widget's STCs. Inactive states accumulate pending bytes; on activation the timer's next tick processes the backlog. Backlog is bounded by `BackgroundProcess`'s existing 256 KB ring-buffer cap per process, so a long-idle state can't grow unbounded. The flush timer lives on the widget (not the state) so we don't pay for N timers; the state has no wx dependency and lives off `LocusTab`, which has no wxWindow parent.
- The state's ANSI parser ([src/frontends/gui/ansi_parser.h](../../src/frontends/gui/ansi_parser.h)) is already stateful across chunks and reentrant-safe per-instance; one parser per process-tab in the state, owned by the state, not the widget.
- Auto-show + user-hide tracking: the user-hid flag belongs on the tab (in the `LocusTab`), not on the state, because state lives as long as the tab does but the flag's semantics are workspace-pane-relative. Set when `LocusFrame::on_terminal_pane_hide()` fires and the active tab had auto-shown the pane; clear when the user re-opens the pane via the View menu.
- Sync `run_command` Run tab: per S5.B the Run tab is cleared on each new sync run (output retained until the next sync run). Per-tab: each tab's state has its own Run tab; clearing happens within that tab's state, not globally. This is the natural consequence of per-tab state and matches the user's mental model.
- Sink routing: `TabServices::process_sink()` returns the **tab's** broker (not the workspace's) so synchronous `RunCommandTool` -- which reaches the panel via `IWorkspaceServices::process_sink()->emit_sync_*` -- lands in the active tab's terminal. `Workspace::process_sink()` stays exposed for symbolic completeness but has no subscribers in v1 (no workspace-level processes today). Per-tab registries each construct their own broker rather than reusing the workspace's.

## Out of scope (v1)

- **AUI perspective persistence for the terminal pane.** Tracked under [S5.Z](S5.Z-misc-gaps.md) alongside the rest of the perspective-save work; the pane keeps the S5.B default of "always starts hidden" until that lands.
- **Per-tab terminal visibility state** (hidden in tab A, shown in tab B). Pane visibility is workspace-level. Considered and rejected -- would force the AUI pane to show/hide on every tab switch, which is jarring and contradicts AUI's docking model.
- **Distinct glyphs for "agent busy" vs. "bg process running"** in the tab title. Reuse the existing `*` badge for both. Add separation if real users report the conflation as confusing.
- **Cross-tab process search** ("find this output across all my tabs"). Per-tab isolation is the point of this stage.
- **Moving a bg process from tab A to tab B.** The process belongs to the tab that spawned it; users can `stop_process` and `run_command_bg` from the new tab if they want to relocate.
- **A dedicated "no commands have run in this tab yet" empty-state panel.** For v1 the terminal pane on a fresh tab just shows an empty `wxAuiNotebook` with no pages. A friendly empty state can be added if the blank pane confuses users.

## Manifest cost

**Zero new tools.** The agent's tool surface is unchanged. This stage is pure frontend restructuring + per-tab fan-out of an existing sink interface.
