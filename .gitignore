build/
dist/
out/
.vs/
.locus/
# S6.2 -- per-archive ZIM index dir (created next to a .zim when opened as a
# workspace; the test fixture run leaves one beside tests/sample_docs/sample.zim).
.locus-zim-*/
*.db
*.gguf
*.onnx
*.user
*.log
CMakeUserPresets.json
third_party/pdfium_download/
tests/integration_tmp/

# Catch2 / test scratch in repo root
/test-results.xml

# Claude Code harness state (per-machine)
.claude/scheduled_tasks.lock
