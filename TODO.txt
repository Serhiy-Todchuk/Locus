- use app icons
- sample.docx has no heading
- tokens per second counter per each turn? (decide after S5.G)
- Shall we put all prompts (system, compaction, etc.) into app folder, or Locus global folder, as a text files, so user can see/edit them?
- Add image attachment for LLM prompt
- If user presses Stop - does it interupts a command the agent is currently running?
- Voice chat input
- FastDebug

Have you added integration test and UI automation test (if needed)?
Run integration tests and then AUI tests. The PC is yours until done.
Run unit tests then integration tests and then AUI tests
Short commit message?
Wrap up — update docs/comments, then we're done.