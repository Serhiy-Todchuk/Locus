- When I send message to LLM in the chat window - then input message field becomes light-themed - only during LLM answering is in progress - then it switches to dark again after answer complete. Please fix so it remains dark-themed. Ask user dialog has white elements as well.
- Add View menu where there are check boxes for closable sub-windows such as File-tree or Details panels (so when I close any panel - I can open it again through this menu)
- How do I open previously saved session in UI app? Add sub-menu to Session menu "Saved sessions" with delete buttons near each session?
- We have context info in chat window - no need to have it in the right corner of main window bottom bar. Better display there last indexing status (for example "indexed 3 files at 9:42", or similiar).
- [Planning] We need to think out how to show tools used by LLM (for example I want to see how many file_reads LLM made for making up specific answer or whether it used any indexed_search)
- [Planning] Isn't the webview for chat UI an overkill? I mean it slightly conficts with Locus philosophy being effecient and eat system resources conservatively. Shall we consider simpler UI text control (which supports colors and bold font)? Is there any in wxWidgets?
- [Planning] Use case: I opened a folder with source files and asked Locus to tell me what it is? It says it executed only "list_dir" tool and answered that it was some library to work with database which is correct, but it took 7k tokens out of 8k max. I suspect it was calling more tools. In any case I need to see (in the UI) where we lost the tokens. There is Details panel dummy UI, but no info in roadmap. Isn't it a good target for implementing it? I want it to be the list of actions histoty - each action is either "system to LLM prompt" (such as info provided by Locus to LLM at the startup, "user to LLM prompt", "LLM to user answer", "LLM runs tool", "finished indexeing 2 files" etc. Also any error or warning is visible there. Each item is expandable - when expanded full info about it accessible. Hovering on it - mid-size info displayed as baloon.
- temperature control in settings; default temperature value is 0.1
- If project is being built in debug config - add Debug menu to UI where there will be 1 items: "Stat" (you can change the name) with all the actions happened such as user sent message to LLM, LLM answered, LLM used tool search_symbols with argument "blabla" and the tool returned "blabla" and all of this with timestamps.
- Are we going to have file viewer
- Tool approval settings
- Create an app icon
- Make tools accessible to user through "/" in the chat window as well as other CLI-"/" commands. when user types "/" show suggestions, when user types more symbols after "/" - update suggestions.
- Is it worth adding llama.cpp for directly running LLMs?
- use direct guthub downloads instead of vcpkgs for third-parties? more cross-platform? more control over dependencies?
- ask_user too with predefined (by LLM) answers as a buttons.
