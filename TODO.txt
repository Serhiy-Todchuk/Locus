- Create an app icon
- Memory bank by Lilu
- Add another tests (functional or integration, don't know correct naming) that I will start manually (or I'll ask you to start) - which will test all the tech-non-UI-features with connecting to LLM as a real usage scenario. This could be separate project - executable within the solution - that would reference locus core. It should fail if no LLM is up and running in LM Studio. Use this folder as testing workspace. This test should include some prompts sent to LLM, asking LLM to read file, find something using semantic/non-semantic search etc. - you think out more test cases. U can make testing wild - create tmp folder in the workspace, create multiple files (use tools), check indexing and embeding works on them, test all tools within tmp, delete it in the end. It is just a suggestion and could be extended or reduced.
- Replace file content tool with such args: start line/pos, end line/pos, what to insert